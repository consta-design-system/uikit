import { Meta } from '@storybook/addon-docs/blocks';
import { Button } from '../Button';

import { 
  ButtonExampleName1, 
  ButtonExampleName2, 
  ButtonExampleName3,
  ButtonExampleName4 
} from './examples/ButtonExampleName/ButtonExampleName';

import { 
  ButtonExampleView1,
  ButtonExampleView2,
  ButtonExampleView3,
  ButtonExampleView4,
  ButtonExampleView5
} from './examples/ButtonExampleView/ButtonExampleView';

import { ButtonExampleForm1, ButtonExampleForm2 } from './examples/ButtonExampleForm/ButtonExampleForm';
import { ButtonExampleSize1, ButtonExampleSize2 } from './examples/ButtonExampleSize/ButtonExampleSize';
import { ButtonExampleIcon1, ButtonExampleIcon2 } from './examples/ButtonExampleIcon/ButtonExampleIcon';
import { ButtonExampleLoading1 } from './examples/ButtonExampleLoading/ButtonExampleLoading';

<Meta title='MDX/Button' component={Button} />

# Button

Кнопка один из самых популярных элементов интерфейса. Компонент Кнопка обладает богатым набором свойств и параметров, сочетания которых покрывают большинство основных сценариев использования.

## Когда использовать

В современных веб-интерфейсах кнопка один из главных элементов. Кнопка может запускать как действие на текущей страницы, так и переход на другую страницу (функция гиперссылки). Разнообразие цветов, форм, размера и других свойств позволяет использовать кнопки для самых разных кейсов и сценариев.

Любую кнопку можно выбрать с клавиатуры с помощью табуляции. Когда кнопка попадает в фокус, вокруг неё появляется рамка. В этом состоянии на кнопку можно нажать с помощью клавиши `Enter`.

## Название

Кнопки используются для обозначения и выбора действий. Например, «Добавить документ», «Удалить запись», «Оплатить налог» и других.

> ✅ Название кнопки пишется с заглавной буквы, не используйте верхний регистр

Название кнопки должно однозначно говорить о том, что произойдет если на нее нажать.

<ButtonExampleName1 />

Для названий кнопок используется совершенный вид инфинитива: «Сохранить», «Скачать отчет», «Найти». Название должно отвечать на вопрос «Что сделать?»

<ButtonExampleName2 />

Не называйте кнопку в две строки: делайте кнопку шире или меняйте название. 

<ButtonExampleName3 />

Нельзя сокращать многоточием название кнопки, если оно не входит в целевом разрешении экрана. Сокращение названия допускается при адаптиве, но рекомендуется использовать подходящие формулировки для всех используемых разрешений экранов.

<ButtonExampleName4 />


## Внешний вид

Кнопка имеет 4 варианта внешнего вида: `primary`, `secondary`, `ghost`, `clear`. Такой набор помогает правильно распределить акцент пользовательских действий на странице. Отвечает за внешний вид параметр `view`.

### Primary

Кнопка призыва к действию (CTA – англ. call to action), в зависимости от ситуации, обычно побуждает пользователей зарегистрироваться/преступить к следующему шагу/залогиниться и т.д. Такие кнопки нужно использовать для самых важных действий, на которые нужно сделать наибольший акцент.

<ButtonExampleView1 />

### Secondary

Secondary кнопка – это как кнопка Primary, только с чуть меньшим акцентом. Используется в ситуациях, когда мы хотим сохранить важность действия, но не слишком привлекать к этому внимание.

<ButtonExampleView2 />

### Ghost

Ghost кнопка – это кнопка «Назад» возле первичной кнопки «Далее». Или кнопка «Отмена» возле кнопки «Подтвердить». Ghost кнопки – это альтернатива Primary действия, которое мы предоставляем пользователям, а также просто действия, на котором мы не хотим делать сильный акцент.

<ButtonExampleView3 />

### Clear

Тип Clear имеет прозрачный фон, но ведет себя как полноценная кнопка. Её нужно использовать, когда внимание пользователя должно быть сосредоточено на более важных контролах. Также кнопки Clear идеально подходят для списка иконочных кнопок третьестепенного действия.

<ButtonExampleView4 />

### Disabled

Все четыре внешних вида имеют одинаковый параметр `disabled`, который делает кнопки одинакого серыми и блокирует интерактивность кнопки, в тех случаях, когда пользователь не может его выполнить в силу каких-то условий. 

<ButtonExampleView5 />


## Размеры

Имеется 4 размера кнопок: `l`, `m`, `s`, `xs`. Ширина кнопки задается размером контента внутри нее и стандартными отступами. 

<ButtonExampleSize1 />

Для каждого из четырёх размеров доступна ширина кнопки в 100% ширины родительского контейнера.

<ButtonExampleSize2 />


### Размер иконки

По умолчанию размер иконки зависит от размера кнопки.

|   |   |   |   |   |
| - | - | - | - | - |
| Размер кнопки | L | M | S  | XS |
| Размер иконки | M | S | XS | XS |

В кнопках могут использоваться иконки разных размеров, в зависимости от ситуации. За это отвечает параметр `iconSize`, в котором можно указать любой размер иконки из списка допустимых: `m`, `s`, `xs` 


## Кнопка с иконкой

Название кнопки можно дополнять иконкой. Она поможет быстрее найти нужную кнопку в ряду других и быстрее понять, что делает кнопка.

Для каждого из четырех размеров кнопки доступны две модификации: `iconLeft` слева и `iconRight` справа от текста. Оба эти параметра ожидают название конкретной иконки из нашей библиотеки [иконок](https://gpn-prototypes.github.io/ui-kit/?path=/docsx/icons--icons).

<ButtonExampleIcon1 />

> ❗️ В панели с большим количеством кнопок, иконки будут создавать лишний визуальный шум. В этом случае оставьте иконки только у самых важных действий

Кнопка может состоять только из иконки. Такие кнопки экономят место и позволяют разместить рядом много похожих кнопок. Например кнопки выбора даты и фильтрации. Важно помнить, что метафора иконки должна быть пользователю знакома или легко считываться сразу, иначе пользователь не поймет что делает кнопка.

<ButtonExampleIcon2 />

Для создания такой кнопки надо использовать булиновый параметр `onlyIcon` в паре с любым из `iconLeft` или `iconRight`


## Форма

За форму кнопки отвечает параметр `form`. Помимо вариант по умолчанию `default`, кнопки могут быть круглыми `round` и квадратными `brick`. 

<ButtonExampleForm1 />

Для комбинации Кнопки с [Полем ввода](https://gpn-prototypes.github.io/ui-kit/?path=/story/textfield--%D1%82%D0%B5%D0%BA%D1%81%D1%82%D0%BE%D0%B2%D0%BE%D0%B5-%D0%BF%D0%BE%D0%BB%D0%B5-%D0%B2%D0%B2%D0%BE%D0%B4%D0%B0) предусмотрены гибридные формы. У таких вариантов правая и левая грани могут отличаться. В примере ниже параметр `form` у Кнопки равен `brick-round`.

<ButtonExampleForm2 />


## Загрузка

В ситуациях когда после нажатия на кнопку пользователь вынужден дождаться какого-то результата в Кнопке можно отобразить лоадер загрузки. 

<ButtonExampleLoading1 />


## Пример использования

```ts
// src/App.ts
import React from 'react';
import { Button } from '@gpn-design/uikit/Button';

const App = () => {
  return <Button label='Сохранить' />
};
```


## Список свойств

| Свойство   | Тип                                                                               | По умолчанию               | Описание  |
| ---------- | --------------------------------------------------------------------------------- | -------------------------- | --------- |
| `view`     | `'clear', 'primary', 'secondary', 'ghost'`                                        | `primary`                  | Внешний вид |
| `size`     | `'xs', 's', 'm', 'l'`                                                             | `m`                        | Размер |
| `form`     | `'default', 'round', 'brick', 'brickRound', 'roundBrick', 'brickDefault', 'defaultBrick'` | `default`                  | Форма |
| `width`    | `'full', 'default'`                                                               | `default`                  | Ширина |
| `label`    | `string`                                                                          | —                          | Текстт в кнопке |
| `as`       | `React.ElementType`                                                               | `button`                   | HTML-тэг кнопки |
| `onClick`  | `React.EventHandler<React.MouseEvent>`                                            | —                          | Действие по клику |
| `iconLeft` | `React.FC<IIcon>`                                                                 | —                          | Иконка слева |
| `iconRight`| `React.FC<IIcon>`                                                                 | —                          | Иконка справа |
| `onlyIcon` | `boolean`                                                                         | `false`                    | Только иконка без текста |
| `iconSize` | `'xs', 's', 'm'`                                                                  | Зависит от размера  кнопки | Размер иконки |
| `loading`  | `boolean`                                                                         | `false`                    | Загрузка |
| `innerRef` | `React.Ref<HTMLElement>`                                                          | —                          | |
| `tabIndex` | `number`                                                                          | —                          | Число определяющее последовательность перехода между элементами интерфейса |
| `className` | `string`                                                                         | —                          | Дополнительные HTML-классы |
| `disabled` | `boolean`                                                                         | `false`                    | Блокировка кнопки |
| `title`    | `string`                                                                          | —                          | HTML-аттрибут title |
