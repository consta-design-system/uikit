import { CheckboxGroupExampleColumn } from './examples/CheckboxGroupExampleDirection/CheckboxGroupExampleDirection';

```tsx
import { CheckboxGroup } from '@consta/uikit/CheckboxGroupDeprecated';
```

Группа чекбоксов, объединённых одним `name`. Пользователь может выбрать несколько вариантов из списка.

<CheckboxGroupExampleColumn />
