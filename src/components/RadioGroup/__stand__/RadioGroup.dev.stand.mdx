import { MdxMenu, MdxTabs } from '@consta/stand';
import { RadioGroupExampleSize } from './examples/RadioGroupExampleSize/RadioGroupExampleSize';
import {
  RadioGroupExampleDisabled,
  RadioGroupExampleDisabledItem,
} from './examples/RadioGroupExampleDisabled/RadioGroupExampleDisabled';
import {
  RadioGroupExampleAlign,
  RadioGroupExampleRow,
  RadioGroupExampleColumn,
} from './examples/RadioGroupExampleAlign/RadioGroupExampleAlign';
import { RadioGroupExampleView } from './examples/RadioGroupExampleView/RadioGroupExampleView';

<MdxMenu>

- [Обзор](#обзор)
- [Импорт](#импорт)
- [Свойства](#свойства)
- [Содержимое](#содержимое)
  - [Название радиокнопки](#название-радиокнопки)
  - [Неактивная радиокнопка](#неактивная-радиокнопка)
  - [Неактивная группа радиокнопок](#неактивная-группа-радиокнопок)
- [Внешний вид](#внешний-вид)
  - [Размер](#размер)
  - [Стиль](#стиль)
  - [Выравнивание](#выравнивание)
  - [Расположение](#расположение)
- [Пример использования](#пример-использования)

</MdxMenu>

## Обзор

Компонент `RadioGroup` предназначен для создания группы радиокнопок, позволяющих пользователю выбрать один вариант из нескольких. Подходит для форм, где требуется однозначный выбор, например, выбор размера, типа или категории. Компонент поддерживает настройку содержимого, внешнего вида и поведения.

## Импорт

```tsx
import { RadioGroup } from '@consta/uikit/RadioGroup';
```

## Свойства

| Свойство                                      | Тип                             | По умолчанию | Описание                                                  |
| --------------------------------------------- | ------------------------------- | ------------ | --------------------------------------------------------- |
| [`items`](#содержимое)                        | `ITEM[]`                        | -            | Массив элементов (радиокнопок)                            |
| [`value`](#поведение)                         | `ITEM`                          | -            | Текущее выбранное значение                                |
| [`onChange`](#поведение)                      | `RadioGroupPropOnChange`        | -            | Действие при выборе элемента                              |
| [`getItemLabel?`](#название-радиокнопки)      | `RadioGroupPropGetItemLabel`    | -            | Функция для получения текста радиокнопки                  |
| [`getItemDisabled?`](#неактивная-радиокнопка) | `RadioGroupPropGetItemDisabled` | -            | Функция для определения неактивного состояния радиокнопки |
| `getItemKey?`                                 | `RadioGroupPropGetItemKey`      | -            | Функция для формирования ключа радиокнопки                |
| [`size?`](#размер)                            | `'xs' \| 's' \| 'm' \| 'l'`     | `'m'`        | Размер группы радиокнопок                                 |
| [`view?`](#стиль)                             | `'primary' \| 'ghost'`          | `'primary'`  | Внешний вид группы радиокнопок                            |
| [`align?`](#выравнивание)                     | `'center' \| 'top'`             | `'center'`   | Выравнивание текста относительно радиокнопок              |
| [`direction?`](#расположение)                 | `'column' \| 'row'`             | `'column'`   | Направление расположения радиокнопок                      |
| [`disabled?`](#неактивная-группа-радиокнопок) | `boolean`                       | `false`      | Делает группу радиокнопок неактивной                      |
| `name?`                                       | `string`                        | -            | Имя `<input>` для доступности                             |
| `className?`                                  | `string`                        | -            | Дополнительные CSS-классы                                 |

## Содержимое

### Название радиокнопки

Название радиокнопки задаётся через свойство `label` в объекте элемента массива `items` или с помощью функции `getItemLabel`. Название должно начинаться с заглавной буквы и не содержать точку в конце.

Пример:

```tsx
const items = [{ name: 'Один' }, { name: 'Два' }, { name: 'Три' }];
```

### Неактивная радиокнопка

Чтобы сделать радиокнопку неактивной, используйте свойство `disabled` в объекте элемента массива `items` или функцию `getItemDisabled`. Неактивные радиокнопки нельзя выбрать.

<MdxTabs>

<RadioGroupExampleDisabledItem />

```tsx
const items = [
  { name: 'Один' },
  { name: 'Два' },
  { name: 'Три' },
  { name: 'Четыре' },
  { name: 'Пять', disabled: true },
];
const [value, setValue] = React.useState(items[0]);

return (
  <RadioGroup
    value={value}
    items={items}
    getItemLabel={(item) => item.name}
    getItemDisabled={(item) => item.disabled}
    onChange={({ value }) => setValue(value)}
  />
);
```

</MdxTabs>

### Неактивная группа радиокнопок

Свойство `disabled` делает всю группу радиокнопок неактивной, отключая взаимодействие с ней. Значение по умолчанию: `false`.

<MdxTabs>

<RadioGroupExampleDisabled />

```tsx
const simpleItems = ['Один', 'Два', 'Три', 'Четыре', 'Пять'];
const [value, setValue] = React.useState(simpleItems[0]);

return (
  <RadioGroup
    value={value}
    items={simpleItems}
    getItemLabel={(item) => item}
    onChange={({ value }) => setValue(value)}
    disabled
  />
);
```

</MdxTabs>

## Внешний вид

### Размер

Свойство `size` определяет размер радиокнопок. Доступные значения: `'xs'`, `'s'`, `'m'`, `'l'`. Значение по умолчанию: `'m'`.

<MdxTabs>

<RadioGroupExampleSize />

```tsx
const simpleItems = ['Один', 'Два', 'Три', 'Четыре', 'Пять'];
const [value, setValue] = React.useState(simpleItems[0]);

return (
  <>
    <RadioGroup
      value={value}
      items={simpleItems}
      getItemLabel={(item) => item}
      onChange={({ value }) => setValue(value)}
      size="l"
    />
    <RadioGroup
      value={value}
      items={simpleItems}
      getItemLabel={(item) => item}
      onChange={({ value }) => setValue(value)}
      size="m"
    />
    <RadioGroup
      value={value}
      items={simpleItems}
      getItemLabel={(item) => item}
      onChange={({ value }) => setValue(value)}
      size="s"
    />
    <RadioGroup
      value={value}
      items={simpleItems}
      getItemLabel={(item) => item}
      onChange={({ value }) => setValue(value)}
      size="xs"
    />
  </>
);
```

</MdxTabs>

### Стиль

Свойство `view` задаёт внешний вид группы радиокнопок. Доступные значения: `'primary'` (по умолчанию) и `'ghost'`.

<MdxTabs>

<RadioGroupExampleView />

```tsx
const simpleItems = ['Один', 'Два', 'Три', 'Четыре', 'Пять'];
const [value, setValue] = React.useState(simpleItems[0]);

return (
  <>
    <RadioGroup
      value={value}
      items={simpleItems}
      getItemLabel={(item) => item}
      onChange={({ value }) => setValue(value)}
      direction="row"
      view="primary"
    />
    <RadioGroup
      value={value}
      items={simpleItems}
      getItemLabel={(item) => item}
      onChange={({ value }) => setValue(value)}
      direction="row"
      view="ghost"
    />
  </>
);
```

</MdxTabs>

### Выравнивание

Свойство `align` определяет выравнивание текста относительно радиокнопок. Доступные значения: `'center'` (по умолчанию) и `'top'`.

<MdxTabs>

<RadioGroupExampleAlign />

```tsx
const alignItems = [
  'Это текст',
  'Это длинный текст',
  'Это очень длинный текст',
  'Это очень-очень длинный текст, которому хочется быть ещё длиннее',
  'Этот текст гораздо длиннее всех предыдущих текстов, что само по себе уже даже странно, хотя на свете бывает много гораздо более странных вещей',
];
const [value, setValue] = React.useState(alignItems[0]);

return (
  <>
    <RadioGroup
      value={value}
      items={alignItems}
      getItemLabel={(item) => item}
      onChange={({ value }) => setValue(value)}
      align="top"
    />
    <RadioGroup
      value={value}
      items={alignItems}
      getItemLabel={(item) => item}
      onChange={({ value }) => setValue(value)}
      align="center"
    />
  </>
);
```

</MdxTabs>

### Расположение

Свойство `direction` задаёт направление расположения радиокнопок. Доступные значения: `'column'` (по умолчанию, вертикально) и `'row'` (горизонтально).

<MdxTabs>

<RadioGroupExampleRow />

```tsx
const simpleItems = ['Один', 'Два', 'Три', 'Четыре', 'Пять'];
const [value, setValue] = React.useState(simpleItems[0]);

return (
  <RadioGroup
    value={value}
    items={simpleItems}
    getItemLabel={(item) => item}
    onChange={({ value }) => setValue(value)}
    direction="row"
  />
);
```

</MdxTabs>

<MdxTabs>

<RadioGroupExampleColumn />

```tsx
const simpleItems = ['Один', 'Два', 'Три', 'Четыре', 'Пять'];
const [value, setValue] = React.useState(simpleItems[0]);

return (
  <RadioGroup
    value={value}
    items={simpleItems}
    getItemLabel={(item) => item}
    onChange={({ value }) => setValue(value)}
    direction="column"
  />
);
```

</MdxTabs>
