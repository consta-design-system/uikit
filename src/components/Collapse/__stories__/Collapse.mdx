import { CollapseExampleView } from './examples/CollapseExampleView/CollapseExampleView';
import { CollapseExampleSize } from './examples/CollapseExampleSize/CollapseExampleSize';

# Collapse

## Внешний вид

<CollapseExampleView />

## Размер

<CollapseExampleSize />

## Свойства

| Свойство             | Тип                                                                          | По умолчанию    | Описание                                                                    |
| -------------------- | ---------------------------------------------------------------------------- | --------------- | --------------------------------------------------------------------------- |
| `label`              | `string`                                                                     | -               | Заголовок коллапса                                                          |
| `size?`              | `'l', 'm', 's', 'xs', '2xs'`                                                 | `'m'`           | Размер                                                                      |
| `view?`              | `'primary', 'secondary'`                                                     | `'primary'`     | Внешний вид                                                                 |
| `withHoverEffect?`   | `boolean`                                                                    | -               | Добавляет эффект при ховере коллапса                                        |
| `withDivider?`       | `boolean`                                                                    | -               | Добавляет разделитель между заголовком коллапса и содержимым                |
| `horizontalPadding?` | `'6xl', '5xl', '4xl', '3xl', '2xl', 'xl', 'l', 'm', 's', 'xs', '2xs', '3xs'` | `'3xs'`         | Горизонтальные отступы в заголовке коллапса                                 |
| `children`           | `React.ReactNode`                                                            | -               | Содержимое коллапса                                                         |
| `icon?`              | `React.FC<IconProps>`                                                        | Стрелочка вниз  | Иконка в закрытом состоянии коллапса                                        |
| `closeIcon?`         | `React.FC<IconProps>`                                                        | Стрелочка вверх | Иконка в открытом состоянии коллапса                                        |
| `iconPosition?`      | `'left', 'right'`                                                            | `'left'`        | Расположение иконки                                                         |
| `rightSide?`         | `React.ReactNode OR React.ReactNode[]`                                       | -               | Дополнительный контент, который можно поместить рядом с заголовком коллапса |
| `isOpen?`            | `boolean`                                                                    | -               | Состояние коллапса (открыт/закрыт)                                          |
| `onClick?`           | `() => void`                                                                 | -               | Действие при клике на коллапс                                               |
| `className?`         | `string`                                                                     | -               | Дополнительный CSS-класс                                                    |

## Пример

```ts
// src/App.ts
import React from 'react';
import { Collapse } from '@consta/uikit/Collapse';

const App = () => {
  return <Collapse label={'Заголовок коллапса'}>Тело коллапса</Collapse>;
};
```
