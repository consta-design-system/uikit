import { DatePickerExampleValue } from './examples/DatePickerExampleValue/DatePickerExampleValue';

```tsx
import { DatePicker } from '@consta/uikit/DatePicker';
```

Это поле со всплывающим календарём, в котором можно выбрать дату или период. Выбранное значение подставляется в поле.

<DatePickerExampleValue />

`DatePicker` сделан из двух других компонентов — [TextField](##LIBS.LIB.STAND/lib:uikit/stand:components-textfield-stable) (поле ввода) и [DateTime](##LIBS.LIB.STAND/lib:uikit/stand:components-datetime-stable) (календарь),
поэтому обладает их свойствами.

> Здесь описаны свойства компонента в целом и не описаны свойства его составляющих.
> Полный список свойств со ссылками на документацию — [в разделе для разработчиков](##LIBS.LIB.STAND.TAB/lib:uikit/stand:components-datepicker-stable/tab:dev/hash:свойства).
