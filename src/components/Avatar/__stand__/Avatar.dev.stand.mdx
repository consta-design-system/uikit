import { MdxMenu } from '@consta/stand';
import { AvatarExampleImage } from './examples/AvatarExampleImage/AvatarExampleImage';
import { AvatarExampleSize } from './examples/AvatarExampleSize/AvatarExampleSize';
import { AvatarExampleForm } from './examples/AvatarExampleForm/AvatarExampleForm';
import { AvatarExampleMonochrome } from './examples/AvatarExampleMonochrome/AvatarExampleMonochrome';

```tsx
import { Avatar } from '@consta/uikit/Avatar';
```

<MdxMenu>

- [Аватар](#аватар)
- [Размер](#размер)
- [Форма](#форма)
- [Монохромный режим](#монохромный-режим)
- [Импорт](#импорт)
- [Список свойств](#свойства)
- [Пример использования](#пример)

</MdxMenu>

## Аватар

Если у вас есть фотография пользователя, укажите ссылку на неё с помощью
`url`.

Если фотографии нет, аватар сформируется из имени пользователя (свойство `name`).
В этом случае цвет аватара выбирается автоматически, повлиять на него не получится.

Если вы не укажете ни то, ни другое, аватар всё равно появится.

```ts
<Avatar url="/static/media/avatar.png" name="Вадим Матвеев" />
<Avatar name="Вадим Матвеев" />
<Avatar name="Куст Геннадий Альбертович" />
<Avatar />
```

<AvatarExampleImage />

## Размер

За размер отвечает свойство `size`.

```ts
<Avatar size='xs' name='...'/>
<Avatar size='s' name='...'/>
<Avatar size='m' name='...'/>
<Avatar size='l' name='...'/>
```

<AvatarExampleSize />

## Форма

За форму отвечает свойство `form`. По умолчанию аватар **круглый**.

```ts
<Avatar form='round' name='...' />
<Avatar form='default' name='...' />
<Avatar form='brick' name='...' />
```

<AvatarExampleForm />

## Монохромный режим

Монохромный режим можно включить через модификацию `monochrome`

```tsx
export const AvatarExampleMonochrome = () => (
  <>
    <Avatar name="Вадим Матвеев" monochrome />
    <Avatar url={avatar} name="Вадим Матвеев" monochrome />
  </>
);
```

<AvatarExampleMonochrome />

## Импорт

```tsx
import { Avatar } from '@consta/uikit/Avatar';
```

## Свойства

| Свойство                            | Тип                            | По умолчанию | Описание                                                         |
| ----------------------------------- | ------------------------------ | ------------ | ---------------------------------------------------------------- |
| [`url?`](#аватар)                   | `string`                       | -            | Ссылка на изображение (аватар пользователя)                      |
| [`name?`](#аватар)                  | `string`                       | -            | Имя пользователя                                                 |
| [`size?`](#размер)                  | `'xs', 's' , 'm', 'l'`         | `'m'`        | Размер аватара                                                   |
| [`form?`](#форма)                   | `'default' , 'round', 'brick'` | `'round'`    | Форма аватара                                                    |
| [`monochrome?`](#монохромный-режим) | `boolean`                      | -            | Включает монохромный режим                                       |
| `className?`                        | `string`                       | -            | Дополнительный CSS-класс                                         |
| `as?`                               | `React.ElementType`            | -            | HTML-тег, в виде которого этот компонент будет представлен в DOM |
| `ref?`                              | `React.Ref<HTMLDivElement>`    | -            | Ссылка на корневой DOM-элемент этого компонента                  |

## Пример

```ts
// src/App.ts
import React from 'react';
import { Avatar } from '@consta/uikit/Avatar';

const App = () => {
  return (
    <Avatar url="https://pbs.twimg.com/profile_images/1150453787603156992/DoiKLDMY_400x400.png" />
  );
};
```
