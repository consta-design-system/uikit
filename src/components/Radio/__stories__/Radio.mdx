import { RadioExampleSize } from './examples/RadioExampleSize/RadioExampleSize';
import { RadioExampleStatus } from './examples/RadioExampleStatus/RadioExampleStatus';
import { RadioExampleMore, RadioExampleOne, RadioExampleChecked } from './examples/RadioExampleUse/RadioExampleUse';

# Radio

Радиокнопка нужна, чтобы пользователь мог сделать выбор — только один из нескольких
вариантов.

- [Название](#название)
- [Как использовать](#как-использовать)
- [Состояния](#состояния)
- [Размер](#размер)
- [Список свойств](#свойства)
- [Пример использования](#пример)

## Название

Пишется с заглавной буквы, точка не нужна.

## Как использовать

### Только один на выбор

Список должен быть таким, чтобы пользователь мог выбрать только один вариант.
Это может быть способ доставки заказа, форма пирога, модель телефона.

Если вы понимаете, что в вашем списке можно выбрать несколько вариантов,
радиокнопки не подойдут. Используйте [Checkbox](#?path=/docs/components-checkbox--playground).

<RadioExampleOne />

### Больше одного варианта

Радиокнопки подходят, если у вас в списке несколько вариантов.
Если вариант только один, возможно, кнопка пользователю не нужно делать выбор,
а значит, и радиокнопка не нужна. Если нужно выбрать один из двух вариантов,
используйте [переключатель](#?path=/docs/components-switch--playground).

<RadioExampleMore />


### Хотя бы один выбранный

Радиокнопки как правило обозначают выбор, без которого нельзя обойтись. Например,
если пользователь не укажет, как доставить товар, вы не сможете придумать за него.
Поэтому обязательно отметьте один пункт выбранным.

<RadioExampleChecked />

## Состояния

`checked` отмечает радиокнопку выбранной.

`disabled` не дает пользователю сделать выбор.


<RadioExampleStatus />


##  Размер

За размер отвечает свойство `size`.

<RadioExampleSize />

```ts
<Radio size="m" label="Размер M" />
<Radio size="l" label="Размер L" />
```

## Свойства

<!-- props:start -->

| Свойство         | Тип                                     | По умолчанию  | Описание                                     |
| ---------------- | --------------------------------------- | ------------- | -------------------------------------------- |
| [`label`](#название)|  `string`                            |   -           | Текст рядом с радиокнопкой    |
| [`size`](#размер)| `'m', 'l'`                              | `'m'`         | Размер радиокнопки                               |
| [`checked`](#состояния)| `boolean`                         | -             | Делает радиокнопку выбранной                 |
| `intermediate`   | `boolean`                               | -             | Показывает радиокнопку с неопределенным статусом |
| [`disabled`](#состояния)| `boolean`                        | -             | Делает радиокнопку неактивной                  |
| `required`       | `boolean`                               | -             | Такую радиокнопку обязательно нужно выбрать    |
| `readOnly`       | `boolean`                               | -             | Радиокнопка, которую нельзя поменять  |
| `autoFocus`      | `boolean`                               | -             | Переносит фокус на эту кнопку  |
| `tabIndex`       | `number`                                | -             | Порядковый номер при переходе между элементами интерфейса по клавише **tab**  |
| `step `          | `number | string`                       | -             |           |
| `name    `       | `string`                                | -             | Имя элемента в DOM |
| `onChange`       | `string`                               | -             | Действие при выборе радиокнопки  |
| `onFocus`        | `React.FocusEventHandler<HTMLInputElement>`| -             | Действие по фокусу   |
| `onBlur`         | `React.FocusEventHandler<HTMLInputElement>` | -             | Действие при потере фокуса   |
| `inputRef`       | `React.FocusEventHandler<HTMLInputElement>`| -          | Ссылка на корневой DOM-элемент этого компонента |

<!-- props:end -->

## Пример

```ts
// src/App.ts
import React from 'react';
import { Radio } from '@gpn-design/uikit/Radio';

const App = () => {
  return <Radio label="I am radio" />;
};
```
