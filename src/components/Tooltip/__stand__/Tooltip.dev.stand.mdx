import { MdxMenu } from '@consta/stand';
import {
  TooltipPositionedCoords,
  TooltipPositionedAnchor,
} from './examples/TooltipPosition/TooltipPosition';

```tsx
import { Tooltip } from '@consta/uikit/Tooltip';
```

<MdxMenu>

- [Тема](#тема-тултипа)
- [Размер](#размер)
- [Статус](#статус)
- [Позиционирование](#позиционирование-тултипа)
  - [По якорю](#по-элементу-якорю)
  - [По координатам](#по-координатам)
- [Импорт](#импорт)
- [Список свойств](#свойства)
- [Пример использования](#примеры)

</MdxMenu>

## Тема тултипа

Содержимое тултипа использует инвертированную тему: в светлой теме тултип будет тёмным, в тёмной — светлым.

## Размер

За размер тултипа отвечает свойство `size`. Варианты: `s`, `m`, `l`. В зависимости от выбранного размера изменятеся максимальная ширина тултипа.

## Статус

Иногда может быть полезно дополнительно выделить статус сообщения в Тултипе, например для сообщения об ошибки в валидации. Сделать это можно с помощью свойства `status` со значениями: `normal`, `success`, `warning` и `alert`.

## Позиционирование тултипа

Для позиционирования тултипа используется компонент [Popover](##LIBS.STAND.TAB/tab:dev/stand:uikit-components-popover-stable/hash:способы-позиционирования) и его способы позиционирования.

### По элементу-якорю

<TooltipPositionedAnchor />

### Пример

```ts
<Button label="Открыть тултип" type="button" ref={buttonRef} />
<Tooltip size="s" anchorRef={buttonRef}>
  Контент тултипа
</Tooltip>
```

### По координатам

<TooltipPositionedCoords />

```tsx
export const TooltipPositionedCoords = () => {
  const [position, setPosition] = useState<Position>(undefined);

  const handleMouseMove = (event: React.MouseEvent) => {
    setPosition({ x: event.clientX, y: event.clientY });
  };

  return (
    <>
      <div
        onMouseMove={handleMouseMove}
        onMouseLeave={() => setPosition(undefined)}
      >
        <Text>Область, в которой работает отслеживание мышки</Text>
      </div>
      <Tooltip
        direction="upCenter"
        spareDirection="downStartLeft"
        possibleDirections={directions}
        size={tooltipPropSizesDefault}
        position={position}
        isInteractive={false}
      >
        <Text size="xs">Текст тултипа</Text>
      </Tooltip>
    </>
  );
};
```

## Импорт

```tsx
import { Tooltip } from '@consta/uikit/Tooltip';
```

## Свойства

| Свойство              | Тип или варианты значения                     | По умолчанию            | Описание                                                                                                      |
| --------------------- | --------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------- |
| `anchorRef?`          | `React.RefObject<HTMLElement>`                | —                       | см. [Popover](##LIBS.STAND.TAB/tab:dev/stand:uikit-components-popover-stable)                                 |
| `position?`           | `{ x: number; y: number }` &#124; `undefined` | —                       | см. [Popover](##LIBS.STAND.TAB/tab:dev/stand:uikit-components-popover-stable/)                                |
| `size`                | `'s', 'm', 'l'`                               | '`m`'                   | Размер, влияющий на максимальную ширину тултипа                                                               |
| `children`            | `React.ReactNode`                             | —                       | Контент тултипа                                                                                               |
| `direction?`          | `Direction`                                   | `'upCenter'`            | см. [Popover](##LIBS.STAND.TAB/tab:dev/stand:uikit-components-popover-stable/hash:направление-разворачивания) |
| `possibleDirections?` | `Direction[]`                                 | все возможные Direction | см. [Popover](##LIBS.STAND.TAB/tab:dev/stand:uikit-components-popover-stable/hash:направление-разворачивания) |
| `spareDirection?`     | `Direction`                                   | `'downStartLeft'`       | см. [Popover](##LIBS.STAND.TAB/tab:dev/stand:uikit-components-popover-stable/hash:направление-разворачивания) |
| `isInteractive?`      | `boolean`                                     | `true`                  | см. [Popover](##LIBS.STAND.TAB/tab:dev/stand:uikit-components-popover-stable)                                 |
| `onClickOutside?`     | `(event: MouseEvent) => void`                 | —                       | см. [Popover](##LIBS.STAND.TAB/tab:dev/stand:uikit-components-popover-stable)                                 |
| `equalAnchorWidth?`   | `boolean`                                     | `fasle`                 | см. [Popover](##LIBS.STAND.TAB/tab:dev/stand:uikit-components-popover-stable)                                 |

### Пример

```ts
<Tooltip size="s" position={{ x: 100, y: 200 }}>
  Контент тултипа
</Tooltip>
```
