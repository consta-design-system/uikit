# CheckboxGroup

Группа чекбоксов, объединённых одним `name`. Пользователь может выбрать несколько вариантов из списка.
Если пользователю необходимо выбрать только один вариант из списка, лучше использовать [RadioGroup](?path=/docs/компоненты-radiogroup--playground).

- [Направление](#направление)
- [Состояние](#состояние)
- [Размер](#размер)
- [Внешний вид](#внешний-вид)
- [Список свойств](#свойства)
- [Пример использования](#пример)

## Направление

Группа чекбоксов может располагаться как по вертикали, так и по горизонтали. За направление отвечает свойство `direction`.

```tsx
<CheckboxGroup
  direction="column"
/>
<CheckboxGroup
  direction="row"
/>
```

## Состояние

`disabled` не дает пользователю сделать выбор.

```tsx
<CheckboxGroup disabled />
```

## Размер

За размер отвечает свойство `size`.

```tsx
<CheckboxGroup size="m" />
<CheckboxGroup size="l" />
```

## Внешний вид

Всего в дизайн-системе два вида групп чекбоксов: `primary` и `ghost`. За это отвечает свойство `view`.

```tsx
<CheckboxGroup view="primary" />
<CheckboxGroup view="ghost" />
```

## Свойства

```
type OnChange = (props: { e: React.ChangeEvent<HTMLInputElement>; value: ITEM[] | null }) => void;
```

<!-- props:start -->

| Свойство                     | Тип                               | По умолчанию | Описание                                                                                                  |
| ---------------------------- | --------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------- |
| `value`                      | `Item[]`                          | —            | Значение                                                                                                  |
| `items`                      | `Item[]`                          | —            | Элементы                                                                                                  |
| `getLabel`                   | `(item: Item) => string`          | —            | Функция для формирования текста для чекбоксов. Также используется в качестве уникального ключа элемента   |
| `getDisabled?`               | `(item: Item) => boolean`         | —            | Функция для определения disabled состояния чекбокса                                                       |
| `onChange`                   | `OnChange`                        | —            | Действие при выборе элемента                                                                              |
| `name`                       | `string`                          | —            | Имя `<input>`, указывается для доступности                                                                |
| [`direction?`](#направление) | `'column', 'row'`                 | `'column'`   | Направление чекбоксов                                                                                     |
| [`disabled?`](#состояние)    | `boolean`                         | `false`      | Делает группу чекбоксов неактивной                                                                        |
| [`size?`](#размер)           | `'m', 'l'`                        | `'m'`        | Размер группы чекбоксов                                                                                   |
| [`view?`](#внешний-вид)      | `'primary', 'ghost'`              | `'primary'`  | Внешний вид                                                                                               |
| `className?`                 | `string`                          | —            | Дополнительные CSS-классы                                                                                 |

<!-- props:end -->

## Пример

```ts
// src/App.ts
import React from 'react';
import { CheckboxGroup } from '@consta/uikit/CheckboxGroup';

type Item = {
  name: string;
  disabled?: boolean;
};

const items: Item[] = [
  { name: 'один' },
  { name: 'два' },
  { name: 'три' },
  { name: 'disabled', disabled: true },
  { name: 'пять' },
];

const App = () => {
  const [value, setValue] = React.useState<Item[] | null>(null);

  return (
    <CheckboxGroup
      value={value}
      items={items}
      getLabel={(item) => item.name}
      getDisabled={(item) => item.disabled}
      onChange={({ value }) => setValue(value)}
      name={'CheckboxGroup'}
    />
  );
};
```
