import { MdxMenu, MdxInformer } from '@consta/stand';
import { NotificationExample } from './examples/NotificationExample/NotificationExample';
import {
  NotificationListExample,
  NotificationListExampleBasic,
} from './examples/NotificationListExample/NotificationListExample';

import { NotificationListExampleActions } from './examples/NotificationListExampleActions/NotificationListExampleActions';
import {
  NotificationListExampleDate,
  NotificationListExampleGroups,
} from './examples/NotificationListExampleGroups/NotificationListExampleGroups';
import { NotificationItemExampleBasic } from './examples/NotificationItemExampleBasic/NotificationItemExampleBasic';
import { NotificationItemExampleBadges } from './examples/NotificationItemExampleBadges/NotificationItemExampleBadges';
import { NotificationItemExampleView } from './examples/NotificationItemExampleView/NotificationItemExampleView';
import { NotificationItemExampleAction } from './examples/NotificationItemExampleAction/NotificationItemExampleAction';
import { NotificationItemExampleRead } from './examples/NotificationItemExampleRead/NotificationItemExampleRead';

```tsx
import { Notification } from '@consta/header/Notification';
```

<MdxMenu>

- [Нотификации](#нотификации)
- [Список уведомлений](#список-уведомлений)
  - [Действия](#действия)
  - [Группировка](#группировка)
    - [По дням](#группировка-по-дням)
    - [По группам](#группировка-по-группам)
  - [Свойства NotificationList](#свойства-notificationlist)
- [Уведомление](#уведомление)
  - [Контент](#контент)
  - [Бейджики](#бейджики)
  - [Изображение или аватар](#изображение-или-аватар)
  - [Действия с уведомлением](#действия-с-уведомлением)
  - [Прочитанные](#прочитанные)
  - [Свойства NotificationItem](#свойства-notificationitem)
- [Мобильная версия](#мобильная-версия)
- [Импорт](#импорт)
- [Список свойств](#свойства)

</MdxMenu>

## Нотификации

Список уведомлений работает так же, как в компоненте [NotificationList](#список-уведомлений).

```tsx
import { IconEye } from '@consta/icons/IconEye';
import { IconTrash } from '@consta/icons/IconTrash';

export const items: NotificationDefaultItem[] = [
  {
    label: 'Иванов Иван Иванович',
    description: 'Добавил файлы в проект, план/факт по расчету предварительные',
    image: 'https://avatars.githubusercontent.com/u/13190808?v=4',
    date: new Date(2021, 10, 12, 13, 57, 0),
    read: false,
    badges: [
      {
        label: 'отчеты 1',
        status: 'normal',
      },
      {
        label: 'файлы 2',
        status: 'warning',
      },
      {
        label: 'система 3',
        status: 'success',
      },
      {
        label: 'отчеты 4',
        status: 'normal',
      },
      {
        label: 'файлы 5',
        status: 'warning',
      },
      {
        label: 'система 6',
        status: 'success',
      },
    ],
    onClick: emptyFunction,
    actions: [
      {
        label: 'Удалить',
        onClick: emptyFunction,
        icon: IconTrash,
      },
      {
        label: 'Отметить как прочитанное',
        onClick: emptyFunction,
        icon: IconEye,
      },
    ],
    view: 'user',
    group: 'd',
  },

  // и так для каждого уведомления
];

export const NotificationExample = () => (
  <Notification
    listClassName={cnNotificationStories('List')}
    items={items}
    title="Уведомления"
    actions={actions}
    groupByDay
  />
);
```

<NotificationExample />

## Список уведомлений

Данные для уведомлений можно указать в свойстве `items` (тип [NotificationListDefaultItem](#свойства-notificationlist)). В него входит:

- `label` — заголовок уведомления (обязательный),
- `description` — описание,
- `image` — ссылка на изображение,
- `read` — отмечает сообщение [прочитанным](#прочитанные),
- `date` — дата,
- `badges` — [список бейджиков](#бейджики),
- `actions` — объект с описанием [действий](#действия-с-уведомлением), которые можно совершить с этим уведомлением,
- `group` — [группа](#группировка), к которой относится это уведомление,
- `view` — [внешний вид](#вид) списка c уведомлениями,
- `onClick` — возвращает клик на этот пункт.

Вот пример простого списка: в нём есть заголовок, описание и картинка, больше ничего:

```tsx
import {
  NotificationList,
  NotificationListDefaultItem,
} from '@consta/header/Notification';

const itemsBasic: NotificationListDefaultItem[] = [
  {
    label: 'Один человек',
    description: 'Принёс краску',
    image: 'https://avatars.githubusercontent.com/u/13190808?v=4',
  },
  {
    label: 'Другой человек',
    description: 'Покрасил забор',
    image: 'https://avatars.githubusercontent.com/u/12581569?s=40&v=4',
  },
];

export const NotificationListExample = () => (
  <NotificationList
    items={itemsBasic}
    title="Уведомления о действиях с забором"
  />
);
```

<NotificationListExampleBasic />

### Действия

Дополнительные действия, которые пользователь может совершать с уведомлениями,
показываются в выпадающем меню — при нажатии на многоточие.

Можно совершать действия [со всем списком уведомлений сразу](#действия-со-списком) или только [с одним уведомлением](#действия-с-уведомлением).

Чтобы создать уведомления, используйте объект типа [NotificationItemAction](#свойства-notificationlist). Что внутри:

- `label` — название действия (например, «Удалить»),
- `icon` — иконка из [библиотеки иконок](##LIBS.LIB.STAND/lib:uikit/stand:components-icon-stable),
- `onClick` — функция для получения клика по этому элементу.

Вы можете создать уведомления с кастомным типом данных, отличающимся от [NotificationListDefaultItem](#свойства-notificationlist). В этом случае используйте геттеры
`getActionLabel`, `getActionIcon`, `getActionOnClick`, чтобы указать компоненту,
откуда брать описание действий.

Действия со списком можно посмотреть по нажатию на многоточие рядом с заголовком списка. Чтобы их добавить, создайте объект с описанием действий и передайте в свойстве `actions`.

```tsx
import {
  NotificationList,
  NotificationListDefaultItem,
} from '@consta/header/Notification';

import { IconTrash } from '@consta/icons/IconTrash';
import { IconEye } from '@consta/icons/IconEye';

const itemsBasic: NotificationListDefaultItem[] = [
  {
    label: 'Один человек',
    description: 'Принёс краску',
    image: 'https://avatars.githubusercontent.com/u/13190808?v=4',
  },
  {
    label: 'Другой человек',
    description: 'Покрасил забор',
    image: 'https://avatars.githubusercontent.com/u/12581569?s=40&v=4',
  },
];

const actions: NotificationItemAction[] = [
  {
    label: 'Отметить все как прочитанное',
    icon: IconEye,
  },
  {
    label: 'Удалить все',
    icon: IconTrash,
  },
];

export const NotificationListExampleActions = () => (
  <NotificationList items={itemsBasic} actions={actions} title="Уведомления" />
);
```

<NotificationListExampleActions />

<MdxInformer status="warning">

Формулируйте описание действий так, чтобы было понятно, что действие применится ко всем элементам списка: «Удалить всё», «Отметить всё», «Наплевать на всё».

</MdxInformer>

### Группировка

За группировку событий отвечает свойство `groups`. Есть два варианта группировки: по дням или по группам.

#### Группировка по дням

Чтобы с группировать уведомления по дням:

1. добавьте дату в объект с описанием каждого уведомления — в атрибуте `date`,
2. укажите свойство `groupByDay`.

```tsx
import {
  NotificationList,
  NotificationListDefaultItem,
} from '@consta/header/NotificationList';

const itemsDate: NotificationListDefaultItem[] = [
  {
    label: 'Первое уведомление',
    description: 'Привет, мир',
    image: 'https://avatars.githubusercontent.com/u/13190808?v=4',
    date: new Date(2021, 10, 12, 13, 57, 0),
  },
  {
    label: 'Второе уведомление',
    description: 'Пока-пока',
    image: 'https://avatars.githubusercontent.com/u/12581569?s=40&v=4',
    date: new Date(2021, 10, 12, 13, 57, 0),
  },
  {
    label: 'Третее уведомление',
    description: 'Пока-пока',
    image: 'https://avatars.githubusercontent.com/u/12581569?s=40&v=4',
    date: new Date(),
  },
  {
    label: 'Четвёртое уведомление',
    description: 'Пока-пока',
    image: 'https://avatars.githubusercontent.com/u/12581569?s=40&v=4',
    date: new Date(),
  },
];

export const NotificationListExampleDate = () => (
  <NotificationList items={itemsDate} groupByDay title="Уведомления" />
);
```

<NotificationListExampleDate />

Чтобы поменять формат даты, используйте свойство `itemDateFormat`.

#### Группировка по группам

Чтобы разбить уведомления на группы:

1. передайте в свойстве `groups` объект с описанием групп типа [NotificationListDefaultGroup](#свойства-notificationlist) с двумя обязательными полями:

- `label` — название группы,
- `id` — идентификатор группы.

2. добавьте идентификатор группы в объект с описанием уведомления — в свойстве `group`.

```tsx
import {
  NotificationList,
  NotificationListDefaultItem,
  NotificationListDefaultGroup,
} from '@consta/header/Notification';

const groups: NotificationListDefaultGroup[] = [
  {
    label: 'важное',
    id: 'important',
  },
  {
    label: 'неважное',
    id: 'not',
  },
];

const itemsGroups: NotificationListDefaultItem[] = [
  {
    label: 'Первое уведомление',
    description: 'Привет, мир',
    image: 'https://avatars.githubusercontent.com/u/13190808?v=4',
    group: 'important',
  },
  {
    label: 'Второе уведомление',
    description: 'Пока-пока',
    image: 'https://avatars.githubusercontent.com/u/12581569?s=40&v=4',
    group: 'not',
  },
  {
    label: 'Третее уведомление',
    description: 'Пока-пока',
    image: 'https://avatars.githubusercontent.com/u/12581569?s=40&v=4',
    group: 'not',
  },
  {
    label: 'Четвёртое уведомление',
    description: 'Пока-пока',
    image: 'https://avatars.githubusercontent.com/u/12581569?s=40&v=4',
    group: 'important',
  },
];

export const NotificationListExampleGroups = () => (
  <NotificationList items={itemsGroups} groups={groups} title="Уведомления" />
);
```

<NotificationListExampleGroups />

### Свойства NotificationList

```tsx
type NotificationListDefaultGroup = {
  label: string;
  id: string;
};

type NotificationListDefaultItem = {
  label: string;
  description?: string;
  image?: string;
  read?: boolean;
  date?: Date;
  badges?: NotificationItemBadge[];
  actions?: NotificationItemAction[];
  onClick?: React.EventHandler<React.MouseEvent>;
  group?: string;
  view?: 'default' | 'user';
};

type NotificationListPropGetItemLabel<ITEM> = (item: ITEM) => string;
type NotificationListPropGetItemDescription<ITEM> = (
  item: ITEM,
) => string | undefined;
type NotificationListPropGetItemImage<ITEM> = (
  item: ITEM,
) => string | undefined;
type NotificationListPropGetItemRead<ITEM> = (
  item: ITEM,
) => boolean | undefined;
type NotificationListPropGetItemDate<ITEM> = (item: ITEM) => Date | undefined;
type NotificationListPropGetItemBadges<ITEM> = (
  item: ITEM,
) => NotificationItemBadge[] | undefined;
type NotificationListPropGetItemActions<ITEM> = (
  item: ITEM,
) => NotificationItemAction[] | undefined;
type NotificationListPropGetItemGroup<ITEM> = (
  item: ITEM,
) => string | undefined;
type NotificationListPropGetItemView<ITEM> = (
  item: ITEM,
) => 'default' | 'user' | undefined;
type NotificationListPropItemDateFormat = (date: Date) => string;
type NotificationListPropGetActionLabel<ACTION> = (action: ACTION) => string;
type NotificationListPropGetActionIcon<ACTION> = (
  action: ACTION,
) => React.FC<IconProps> | undefined;
type NotificationListPropGetActionOnClick<ACTION> = (
  action: ACTION,
) => React.EventHandler<React.MouseEvent> | undefined;

type NotificationListPropGetGroupLabel<GROUP> = (group: GROUP) => string;
type NotificationListPropGetGroupId<GROUP> = (group: GROUP) => string | number;
type NotificationListPropGroupLabelFormat<GROUP_BY_DAY> =
  GROUP_BY_DAY extends true ? (timestamp: number) => string : never;

const defaultGetActionIcon: NotificationListPropGetActionIcon<
  NotificationItemAction
> = (action) => action.icon;
const defaultGetActionLabel: NotificationListPropGetActionLabel<
  NotificationItemAction
> = (action) => action.label;
const defaultGetActionOnClick: NotificationListPropGetActionOnClick<
  NotificationItemAction
> = (action) => action.onClick;
const defaultGetGroupId: NotificationListPropGetGroupId<
  NotificationListDefaultGroup
> = (group) => group.id;
const defaultGetGroupLabel: NotificationListPropGetGroupLabel<
  NotificationListDefaultGroup
> = (group) => group.label;
const defaultGetItemActions: NotificationListPropGetItemActions<
  NotificationListDefaultItem
> = (item) => item.actions;
const defaultGetItemBadges: NotificationListPropGetItemBadges<
  NotificationListDefaultItem
> = (item) => item.badges;
const defaultGetItemDate: NotificationListPropGetItemDate<
  NotificationListDefaultItem
> = (item) => item.date;
const defaultGetItemDescription: NotificationListPropGetItemDescription<
  NotificationListDefaultItem
> = (item) => item.description;
const defaultGetItemGroup: NotificationListPropGetItemGroup<
  NotificationListDefaultItem
> = (item) => item.group;
const defaultGetItemImage: NotificationListPropGetItemImage<
  NotificationListDefaultItem
> = (item) => item.image;
const defaultGetItemLabel: NotificationListPropGetItemLabel<
  NotificationListDefaultItem
> = (item) => item.label;
const defaultGetItemRead: NotificationListPropGetItemRead<
  NotificationListDefaultItem
> = (item) => item.read;
const defaultGetItemView: NotificationListPropGetItemView<
  NotificationListDefaultItem
> = (item) => item.view;

const NotificationListDefaultGroupLabelFormat: NotificationListPropGroupLabelFormat<
  true
> = (timestamp) => {
  if (isToday(timestamp)) {
    return 'Сегодня';
  }
  if (isYesterday(timestamp)) {
    return 'Вчера';
  }
  return format(timestamp, 'dd.MM.yyyy');
};

const defaultDateFormat = (date: Date): string => {
  const currentDate = new Date();

  if (currentDate.getTime() - date.getTime() < 3600000) {
    return formatDistanceToNow(date, { locale: ruLocale, addSuffix: true });
  }

  if (format(currentDate, 'dd.MM.yyyy') === format(date, 'dd.MM.yyyy')) {
    return format(date, 'HH:mm');
  }

  return format(date, 'dd.MM.yyyy HH:mm');
};
```

| Свойство                            | Тип                                           | По умолчанию                              | Описание                                             |
| ----------------------------------- | --------------------------------------------- | ----------------------------------------- | ---------------------------------------------------- |
| [`items`](#список-уведомлений)      | `ITEM[]`                                      | -                                         | Элементы                                             |
| [`actions?`](#действия)             | `ACTIONS[]`                                   | -                                         | Действия списка                                      |
| [`groupByDay?`](#группировка)       | `boolean`                                     | -                                         | Групировка элементов по дням                         |
| [`groupLabelFormat?`](#группировка) | `NotificationListPropGroupLabelFormat`        | `NotificationListDefaultGroupLabelFormat` | Групировка элементов по дням                         |
| [`groups?`](#группировка)           | `GROUP_BY_DAY extends true ? never : GROUP[]` | -                                         | Группы                                               |
| `title?`                            | `string`                                      | -                                         | Заголовок                                            |
| `onClose?`                          | `React.EventHandler<React.MouseEvent>`        | -                                         | Функция события клика на кнопку около заголовка      |
| `itemDateFormat`                    | `NotificationListPropItemDateFormat`          | `defaultDateFormat`                       | Функция форматирования даты элемента                 |
| `getItemLabel?`                     | `NotificationListPropGetItemLabel`            | `defaultGetItemLabel`                     | Функция для определения заголовка элемента           |
| `getItemDescription?`               | `NotificationListPropGetItemDescription`      | `defaultGetItemDescription`               | Функция для определения описания элемента            |
| `getItemImage?`                     | `NotificationListPropGetItemImage`            | `defaultGetItemImage`                     | Функция для определения изображения элемента         |
| `getItemRead?`                      | `NotificationListPropGetItemRead`             | `defaultGetItemRead`                      | Функция для определения статуса "прочитано" элемента |
| `getItemDate?`                      | `NotificationListPropGetItemDate`             | `defaultGetItemDate`                      | Функция для определения даты элемента                |
| `getItemBadges?`                    | `NotificationListPropGetItemBadges`           | `defaultGetItemBadges`                    | Функция для определения бейджов элемента             |
| `getItemActions?`                   | `NotificationListPropGetItemActions`          | `defaultGetItemActions`                   | Функция для определения действий элемента            |
| `getItemGroup?`                     | `NotificationListPropGetItemGroup`            | `defaultGetItemGroup`                     | Функция для определения группы элемента              |
| `getItemView?`                      | `NotificationListPropGetItemView`             | `defaultGetItemView`                      | Функция для определения вида элемента                |
| [`getActionLabel?`](#действия)      | `NotificationListPropGetActionLabel`          | `defaultGetActionLabel`                   | Функция для определения заголовка действия           |
| [`getActionIcon?`](#действия)       | `NotificationListPropGetActionIcon`           | `defaultGetActionIcon`                    | Функция для определения иконки действия              |
| [`getActionOnClick?`](#действия)    | `NotificationListPropGetActionOnClick`        | `defaultGetActionOnClick`                 | Функция для определения функчии действия             |
| [`getGroupLabel?`](#группировка)    | `NotificationListPropGetGroupLabel`           | `defaultGetGroupLabel`                    | Функция для определения заголовка группы             |
| [`getGroupId?`](#группировка)       | `NotificationListPropGetGroupId`              | `defaultGetGroupId`                       | Функция для определения уникального ключа группы     |
| `className?`                        | `string`                                      | -                                         | Дополнительный CSS-класс                             |
| `ref?`                              | `React.Ref<HTMLDivElement>`                   | -                                         | Ссылка на корневой DOM-элемент                       |

## Уведомление

### Контент

Основные элементы уведомления задаются через данные свойства:

- `title` — заголовок уведомления (обязательный),
- `description` — описание,
- `imageUrl` — ссылка на изображение,
- `date` — дата.

```tsx
const date = new Date(2021, 10, 12, 13, 57, 0);

export const NotificationItemExampleBasic = () => {
  return (
    <NotificationItem
      title="Заголовок"
      date={date}
      description="Описание"
      imageUrl="https://avatars.githubusercontent.com/u/12581569?s=40&v=4"
    />
  );
};
```

<NotificationItemExampleBasic />

### Бейджики

Если вам нужно добавить к уведомлению бейджики — с тегами или статусами,
передайте в свойстве `badges` массив с данными типа [`NotificationItemBadge`](#свойства-notificationitem). Что внутри:

- `label` — надпись на бейджике (обязательно),
- `status` — статус бейджика, варианты: `'success'`, `'error'`, `'warning'`, `'normal'`, `'system'` (см. [Badge](##LIBS.LIB.STAND/lib:uikit/stand:components-badge-stable)). По умолчанию `'normal'`.

```tsx
import { NotificationItem } from '@consta/header/Notification';

export const NotificationItemExampleBadges = () => (
  <NotificationItem
    title="Один человек"
    description="Принёс краску"
    imageUrl="https://avatars.githubusercontent.com/u/13190808?v=4"
    badges={[
      {
        label: 'готов к работе',
        status: 'warning',
      },
      {
        label: 'горит',
        status: 'error',
      },
    ]}
  />
);
```

<NotificationItemExampleBadges />

### Изображение или аватар

По умолчанию уведомление ведёт себя как обычный текст с заголовком и изображением.
Если укажете `view='user'`, уведомление будет вести себя как компонент [User](##LIBS.LIB.STAND/lib:uikit/stand:components-user-stable): `title` превратится в имя пользователя, а `imageUrl` — в его аватарку.

Это не сильно влияет на весь компонент, но если не укажете картинку, аватарка сформируется из имени пользователя (цвет подбирается автоматически).

При `view='default'` изображение ничем не заменяется.

```tsx
import { NotificationItem } from '@consta/header/Notification';

export const NotificationItemExampleView = () => (
  <>
    <NotificationItem
      title="Иванов Иван Иванович"
      description="Принёс краску"
      view="default"
    />
    <NotificationItem
      title="Петров Петр Петрович"
      description="Покрасил забор"
      view="user"
    />
  </>
);
```

<NotificationItemExampleView />

### Действия с уведомлением

Дополнительные действия, которые пользователь может совершать с уведомлением,
показываются в выпадающем меню — при нажатии на многоточие.

Описать эти действия можно в свойстве `actions` типа [NotificationItemAction](#свойства-notificationitem).

```tsx
import { NotificationItem } from '@consta/header/Notification';

import { IconTrash } from '@consta/icons/IconTrash';
import { IconEye } from '@consta/icons/IconEye';

export const NotificationItemExampleAction = () => {
  return (
    <NotificationItem
      title="Иванов Иван Иванович"
      description="Принёс краску"
      imageUrl="https://avatars.githubusercontent.com/u/12581569?s=40&v=4"
      actions={[
        {
          label: 'Удалить',
          icon: IconTrash,
        },
        {
          label: 'Отметить как прочитанное',
          icon: IconEye,
        },
      ]}
    />
  );
};
```

<NotificationItemExampleAction />

### Прочитанные

Чтобы пометить сообщение прочитанным, определите флаг `read`.

```tsx
import { NotificationItem } from '@consta/header/Notification';

export const NotificationItemExampleRead = () => (
  <>
    <NotificationItem
      title="Иванов Иван Иванович"
      description="Принёс краску"
      read={true}
    />
    <NotificationItem
      title="Петров Петр Петрович"
      description="Покрасил забор"
      read={false}
    />
  </>
);
```

<NotificationItemExampleRead />

### Свойства NotificationItem

```tsx
export type NotificationItemAction = {
  label: string;
  onClick?: React.EventHandler<React.MouseEvent>;
  icon?: React.FC<IconProps>;
};

export type NotificationItemBadge = {
  label: string;
  status?: BadgePropStatus;
};
```

| Свойство                               | Тип                                    | По умолчанию | Описание                                     |
| -------------------------------------- | -------------------------------------- | ------------ | -------------------------------------------- |
| [`title`](#контент)                    | `string`                               | -            | Заголовок уведомления                        |
| [`description?`](#контент)             | `string`                               | -            | Описание уведомления                         |
| [`imageUrl?`](#изображение-или-аватар) | `string`                               | -            | Изображение уведомления                      |
| [`read?`](#прочитанные)                | `boolean`                              | `false`      | Флаг прочитано уведомление или нет           |
| [`date?`](#контент)                    | `Date`                                 | -            | Дата уведомления                             |
| [`badges?`](#бейджики)                 | `NotificationItemBadge`                | -            | Бейджики уведомления                         |
| [`actions?`](#действия-с-уведомлением) | `NotificationItemAction`               | -            | Действия уведомления                         |
| `dateFormat?`                          | `(date: Date) => string`               | -            | Функция форматирования даты                  |
| [`view?`](#изображение-или-аватар)     | `'user'`<code>&#124;</code>`'default'` | `default`    | Внешний вид                                  |
| `onClick?`                             | `React.EventHandler<React.MouseEvent>` | -            | Оброботчик события клика на уведомление      |
| `actionsMenuOpened?`                   | `boolean`                              | `false`      | Флаг раскрыт ли список действий по умолчанию |
| `className?`                           | `string`                               | -            | Дополнительный CSS-класс                     |
| `ref?`                                 | `React.Ref<HTMLDivElement>`            | -            | Ссылка на корневой DOM-элемент               |

## Мобильная версия

Чтобы уведомления правильно отображалось на мобильном устройстве, добавьте `isMobile={true}`. Тогда список уведомлений откроется на весь экран.

## Импорт

```tsx
import {
  Notification,
  NotificationList,
  NotificationItem,
} from '@consta/header/Notification';
```

## Свойства

| Свойство                            | Тип                                           | По умолчанию                              | Описание                                                                                                  |
| ----------------------------------- | --------------------------------------------- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| [`items`](#список-уведомлений)      | `ITEM[]`                                      | -                                         | Элементы (см. [NotificationList](#свойства-notificationlist))                                             |
| [`actions?`](#действия)             | `ACTIONS[]`                                   | -                                         | Действия списка (см. [NotificationList](#свойства-notificationlist))                                      |
| [`groupByDay?`](#группировка)       | `boolean`                                     | -                                         | Групировка элементов по дням (см. [NotificationList](#свойства-notificationlist))                         |
| [`groupLabelFormat?`](#группировка) | `NotificationListPropGroupLabelFormat`        | `NotificationListDefaultGroupLabelFormat` | Групировка элементов по дням (см. [NotificationList](#свойства-notificationlist))                         |
| [`groups?`](#группировка)           | `GROUP_BY_DAY extends true ? never : GROUP[]` | -                                         | Группы (см. [NotificationList](#свойства-notificationlist))                                               |
| `title?`                            | `string`                                      | -                                         | Заголовок (см. [NotificationList](#свойства-notificationlist))                                            |
| `itemDateFormat`                    | `NotificationListPropItemDateFormat`          | `defaultDateFormat`                       | Функция форматирования даты элемента (см. [NotificationList](#свойства-notificationlist))                 |
| `getItemLabel?`                     | `NotificationListPropGetItemLabel`            | `defaultGetItemLabel`                     | Функция для определения заголовка элемента (см. [NotificationList](#свойства-notificationlist))           |
| `getItemDescription?`               | `NotificationListPropGetItemDescription`      | `defaultGetItemDescription`               | Функция для определения описания элемента (см. [NotificationList](#свойства-notificationlist))            |
| `getItemImage?`                     | `NotificationListPropGetItemImage`            | `defaultGetItemImage`                     | Функция для определения изображения элемента (см. [NotificationList](#свойства-notificationlist))         |
| `getItemRead?`                      | `NotificationListPropGetItemRead`             | `defaultGetItemRead`                      | Функция для определения статуса "прочитано" элемента (см. [NotificationList](#свойства-notificationlist)) |
| `getItemDate?`                      | `NotificationListPropGetItemDate`             | `defaultGetItemDate`                      | Функция для определения даты элемента (см. [NotificationList](#свойства-notificationlist))                |
| `getItemBadges?`                    | `NotificationListPropGetItemBadges`           | `defaultGetItemBadges`                    | Функция для определения бейджов элемента (см. [NotificationList](#свойства-notificationlist))             |
| `getItemActions?`                   | `NotificationListPropGetItemActions`          | `defaultGetItemActions`                   | Функция для определения действий элемента (см. [NotificationList](#свойства-notificationlist))            |
| `getItemGroup?`                     | `NotificationListPropGetItemGroup`            | `defaultGetItemGroup`                     | Функция для определения группы элемента (см. [NotificationList](#свойства-notificationlist))              |
| `getItemView?`                      | `NotificationListPropGetItemView`             | `defaultGetItemView`                      | Функция для определения вида элемента (см. [NotificationList](#свойства-notificationlist))                |
| [`getActionLabel?`](#действия)      | `NotificationListPropGetActionLabel`          | `defaultGetActionLabel`                   | Функция для определения заголовка действия (см. [NotificationList](#свойства-notificationlist))           |
| [`getActionIcon?`](#действия)       | `NotificationListPropGetActionIcon`           | `defaultGetActionIcon`                    | Функция для определения иконки действия (см. [NotificationList](#свойства-notificationlist))              |
| `getActionOnClick?`](#действия)     | `NotificationListPropGetActionOnClick`        | `defaultGetActionOnClick`                 | Функция для определения функчии действия (см. [NotificationList](#свойства-notificationlist))             |
| [`getGroupLabel?`](#группировка)    | `NotificationListPropGetGroupLabel`           | `defaultGetGroupLabel`                    | Функция для определения заголовка группы (см. [NotificationList](#свойства-notificationlist))             |
| [`getGroupId?`](#группировка)       | `NotificationListPropGetGroupId`              | `defaultGetGroupId`                       | Функция для определения уникального ключа группы (см. [NotificationList](#свойства-notificationlist))     |
| [`isMobile?`](#мобильная-версия)    | `boolean`                                     | -                                         | Мобильный вид                                                                                             |
| `className?`                        | `string`                                      | -                                         | Дополнительный CSS-класс для кнопки, по которой открываются уведомления                                   |
| `listClassName?`                    | `string`                                      | -                                         | Дополнительный CSS-класс списка уведомлений (см. [NotificationList](#свойства-notificationlist))          |
| `ref?`                              | `React.Ref<HTMLButtonElement>`                | -                                         | Ссылка на корневой DOM-элемент (см. [NotificationList](#свойства-notificationlist))                       |
