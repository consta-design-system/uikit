import { TooltipPositionedAnchor } from './examples/TooltipPosition/TooltipPosition';

```tsx
import { Tooltip } from '@consta/uikit/Tooltip';
```

Тултип — компонент для отображения всплывающего окна возле элемента или определённой точки на экране.

Содержимое тултипа использует инвертированную тему: в светлой теме тултип будет тёмным, в темной — светлым.

<TooltipPositionedAnchor />
