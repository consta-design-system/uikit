import { MdxMenu, MdxTabs } from '@consta/stand';
import {
  LoaderExampleSize,
  LoaderExampleType,
  LoaderExampleView,
} from './examples/LoaderExampleSize/LoaderExampleSize';
import { LoaderExampleCustomColor } from './examples/LoaderExampleCustomColor/LoaderExampleCustomColor';
import { LoaderExampleCustomSize } from './examples/LoaderExampleCustomSize/LoaderExampleCustomSize';

```tsx
import { Loader } from '@consta/uikit/Loader';
```

<MdxMenu>

- [Свойства](#свойства)
- [Внешний вид](#внешний-вид)
  - [Тип](#тип)
  - [Размер](#размер)
  - [Стиль](#стиль)
  - [Кастомный цвет](#кастомный-цвет)

</MdxMenu>

## Свойства

| Свойство           | Тип                                                        | По умолчанию | Описание                                        |
| ------------------ | ---------------------------------------------------------- | ------------ | ----------------------------------------------- |
| [`type?`](#тип)    | `'dots'` <code>&#124;</code> `'circle'`                    | `dots'`      | Тип                                             |
| [`size?`](#размер) | `'xs'` <code>&#124;</code> `'s'` <code>&#124;</code> `'m'` | `'m'`        | Размер                                          |
| [`view?`](#тип)    | `'primary'` <code>&#124;</code> `'clear'`                  | `primary'`   | Вид                                             |
| `className?`       | `string`                                                   | -            | Дополнительный CSS-класс                        |
| `ref?`             | `React.Ref<HTMLDivElement>`                                | -            | Ссылка на корневой DOM-элемент этого компонента |

### CSS-переменные

| Свойство                                  | Описание          |
| ----------------------------------------- | ----------------- |
| [ `--loader-color-prop`](#кастомный-цвет) | Установка цвета   |
| [`--loader-size-prop`](#размер)           | Установка размера |

## Внешний вид

### Тип

Тип компонента задается в свойстве `type`. Возможные значения:

- `'dots'` - в виде точек,
- `'circle'` - в виде круга.

<LoaderExampleType />

### Размер

За размер компонента отвечает свойство `size`.

<LoaderExampleSize />

Чтобы установить любой размер, используйте css-переменную `--loader-size-prop`

<MdxTabs>

```tsx
import './LoaderExampleCustomSize.css';

import React from 'react';
import { cn } from '##/utils/bem';
import { Loader } from '@consta/uikit/Loader';

const cnLoaderExampleCustomSize = cn('LoaderExampleCustomSize');

export const LoaderExampleCustomSize = () => (
  <Loader className={cnLoaderExampleCustomSize()} />
);
```

```css
.LoaderExampleCustomSize {
  --loader-size-prop: 70px;
}
```

<LoaderExampleCustomSize />

</MdxTabs>

### Стиль

Стиль компонента можно установить в свойстве `view`. Доступные значения: `primary` и `clear`.

<LoaderExampleView />

### Кастомный цвет

Для перекрашивания компонента в любой цвет используйте css-переменную `--loader-color-prop`.

<MdxTabs>

```tsx
import './LoaderExampleCustomColor.css';

import React from 'react';
import { cn } from '##/utils/bem';
import { Loader } from '@consta/uikit/Loader';

const cnLoaderExampleCustomColor = cn('LoaderExampleCustomColor');

export const LoaderExampleCustomColor = () => (
  <Loader type="circle" className={cnLoaderExampleCustomColor()} />
);
```

```css
.LoaderExampleCustomColor {
  --loader-color-prop: var(--color-typo-primary);
}
```

<LoaderExampleCustomColor />

</MdxTabs>
