import { LoaderExampleSizeM } from './examples/LoaderExampleSize/LoaderExampleSize';
import { LoaderExampleSizeS } from './examples/LoaderExampleSize/LoaderExampleSize';

# Loader

Прелоадер показывает, что информация загружается.

- [Размер](#размер)
- [Список свойств](#свойства)
- [Пример использования](#пример)

## Размер

За размер прелоадера отвечает свойство `size`.

| Размер M               | Размер S               |
| ---------------------- | ---------------------- |
| <LoaderExampleSizeM /> | <LoaderExampleSizeS /> |

## Свойства

<!-- props:start -->

| Свойство          | Тип                         | По умолчанию | Описание                                        |
| ----------------- | --------------------------- | ------------ | ----------------------------------------------- |
| [`size`](#размер) | `'s', 'm'`                  | `'m'`        | Размер прелоадера                               |
| `className`       | `string`                    | —            | Дополнительный CSS-класс                        |
| `ref`             | `React.Ref<HTMLDivElement>` | —            | Ссылка на корневой DOM-элемент этого компонента |

<!-- props:end -->

## Пример

```ts
// src/App.ts
import React from 'react';
import { Loader } from '@consta/uikit/Loader';

const App = () => {
  return <Loader />;
};
```
