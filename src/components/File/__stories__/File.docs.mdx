import { FileExampleExtension } from './examples/FileExampleExtension/FileExampleExtension';
import { FileExampleLoading } from './examples/FileExampleLoading/FileExampleLoading';
import { FileExampleSize } from './examples/FileExampleSize/FileExampleSize';

# File

Этот компонент формирует красивую иконку для файла. Можно использовать во всех случаях,
когда нужно показать файл — один или в списке.

> Для файла, который загружает пользователь, есть специальный компонент —
> [Attachment](?path=/story/components-attachment--playground)

- [Иконка](#иконка)
- [Размер иконки](#размер-иконки)
- [Индикаторы загрузки](#индикаторы-загрузки)
- [Свойства](#свойства)
- [Пример использования](#пример-использования)

## Иконка

Укажите расширение файла — с помощью свойства `extension` — и пользователь увидит
нужную иконку. Если расширения нет в нашей в нашей галерее иконок (во вкладке **Gallery**),
на иконке будет знак вопроса.

```ts
<File extension="doc" />
<File extension="docx" />
<File extension="jpg" />
<File extension="mov" />
<File extension="BlaBla" />
```

<FileExampleExtension />

## Размер иконки

За размер иконки отвечает свойство `size`.

```ts
<File size="m" />
<File size="s" />
```

<FileExampleSize />

## Индикаторы загрузки

Свойство `loading` показывает, что файл загружается.

Если нужен индикатор загрузки, добавьте `loadingWithProgressSpin`.

Процент загрузки можно указать с помощью `loadingProgress`.

```ts
<File loading />
<File loading loadingWithProgressSpin />
<File loading loadingWithProgressSpin loadingProgress={30} />
<File loading loadingWithProgressSpin loadingProgress={60} />
<File loading loadingWithProgressSpin loadingProgress={90} />
```

<FileExampleLoading />

## Свойства

| Свойство                                          | Тип                         | По умолчанию | Описание                                                         |
| ------------------------------------------------- | --------------------------- | ------------ | ---------------------------------------------------------------- |
| [`extension`](#иконка)                            | `string`                    | -            | Иконка файла                                                     |
| [`size`](#размер-иконки)                          | `'m', 's'`                  | `'m'`        | Размер иконки                                                    |
| [`loading`](#индикаторы-загрузки)                 | `boolean`                   | -            | Показывает, что файл загружается                                 |
| [`loadingWithProgressSpin`](#индикаторы-загрузки) | `boolean`                   | -            | Показывает индикатор прогресса загрузки                          |
| [`loadingProgress`](#индикаторы-загрузки)         | `number`                    | -            | Прогресс загрузки в процентах                                    |
| `as`                                              | keyof JSX.IntrinsicElements | -            | HTML-тег, в виде которого этот компонент будет представлен в DOM |
| `className`                                       | `string`                    | -            | Дополнительный класс                                             |
| `ref`                                             | `React.Ref<HTMLDivElement>` | -            | Ссылка на корневой DOM-элемент компонента                        |

## Пример использования

```ts
// src/App.ts
import React from 'react';
import { File } from '@consta/uikit/File';

const App = () => {
  return <File extension="doc" />;
};
```
