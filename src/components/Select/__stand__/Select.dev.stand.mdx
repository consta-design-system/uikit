import { MdxMenu, MdxInformer, MdxTabs } from '@consta/stand';
import { SelectExampleItems } from './examples/SelectExampleItems/SelectExampleItems';
import { SelectExamplePlaceholder } from './examples/SelectExamplePlaceholder/SelectExamplePlaceholder';
import { SelectExampleSize } from './examples/SelectExampleSize/SelectExampleSize';
import { SelectExampleForm } from './examples/SelectExampleForm/SelectExampleForm';
import { SelectExampleView } from './examples/SelectExampleView/SelectExampleView';
import { SelectExampleRenderItem } from './examples/SelectExampleRenderItem/SelectExampleRenderItem';
import { SelectExampleRenderValue } from './examples/SelectExampleRenderValue/SelectExampleRenderValue';
import { SelectExampleDisabled } from './examples/SelectExampleDisabled/SelectExampleDisabled';
import { SelectExampleDisabledItem } from './examples/SelectExampleDisabledItem/SelectExampleDisabledItem';
import { SelectExampleGroups } from './examples/SelectExampleGroups/SelectExampleGroups';
import { SelectExampleVirtualScroll } from './examples/SelectExampleVirtualScroll/SelectExampleVirtualScroll';
import { SelectExampleIsLoading } from './examples/SelectExampleIsLoading/SelectExampleIsLoading';
import { SelectExampleDropdownOpen } from './examples/SelectExampleDropdownOpen/SelectExampleDropdownOpen';
import {
  SelectExampleCustomTypes,
  SelectExampleCustomTypesWithGroups,
} from './examples/SelectExampleCustomTypes/SelectExampleCustomTypes';
import {
  SelectExampleLabel,
  SelectExampleLabelIcon,
} from './examples/SelectExampleLabel/SelectExampleLabel';
import { SelectExampleStatus } from './examples/SelectExampleStatus/SelectExampleStatus';

<MdxMenu>

- [Обзор](#обзор)
- [Импорт](#импорт)
- [Свойства](#свойства)
- [Содержимое](#содержимое)
  - [Варианты](#варианты)
  - [Выбранное значение](#выбранное-значение)
  - [Лейбл и подпись](#лейбл-и-подпись)
  - [Подсказка](#подсказка)
  - [Неактивный элемент](#неактивный-элемент)
  - [Группы вариантов](#группы-вариантов)
- [Внешний вид](#внешний-вид)
  - [Размер](#размер)
  - [Форма](#форма)
  - [Стиль](#стиль)
  - [Статус поля](#статус-поля)
- [Поведение](#поведение)
  - [Неактивный выпадающий список](#неактивный-выпадающий-список)
  - [Управление состоянием выпадающего списка](#управление-состоянием-выпадающего-списка)
  - [Индикация загрузки](#индикация-загрузки)
  - [Виртуализация списка](#виртуализация-списка)
- [Кастомизация](#кастомизация)
  - [Кастомный элемент списка](#кастомный-элемент-списка)
  - [Кастомное выбранное значение](#кастомное-выбранное-значение)
  - [Кастомные типы данных](#кастомные-типы-данных)
- [Пример использования](#пример)

</MdxMenu>

## Обзор

Компонент **Select** — это выпадающий список, который позволяет пользователю выбрать один вариант из предложенных. Поддерживает настройку внешнего вида, содержимого, поведения и кастомизацию элементов.

## Импорт

```tsx
import { Select } from '@consta/uikit/Select';
```

## Свойства

| Свойство                                                                | Тип или варианты значения                                                                                                                                         | По умолчанию              | Описание                                          |
| ----------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------- |
| [`items`](#варианты)                                                    | `ITEM[]`                                                                                                                                                          | -                         | Варианты для выбора                               |
| [`value?`](#выбранное-значение)                                         | `ITEM`, `null`                                                                                                                                                    | -                         | Выбранное значение                                |
| [`onChange?`](#выбранное-значение)                                      | `SelectPropOnChange<ITEM>`                                                                                                                                        | -                         | Обработчик изменения значения                     |
| [`label?`](#лейбл-и-подпись)                                            | `string`                                                                                                                                                          | -                         | Лейбл компонента                                  |
| [`labelPosition?`](#лейбл-и-подпись)                                    | `'top'`, `'left'`                                                                                                                                                 | `'top'`                   | Положение лейбла                                  |
| [`caption?`](#лейбл-и-подпись)                                          | `string`                                                                                                                                                          | -                         | Подпись под компонентом                           |
| [`labelIcon?`](#лейбл-и-подпись)                                        | `React.ReactNode`                                                                                                                                                 | -                         | Иконка для лейбла                                 |
| [`placeholder?`](#подсказка)                                            | `string`                                                                                                                                                          | -                         | Подсказка, отображается, когда вариант не выбран  |
| [`groups?`](#группы-вариантов)                                          | `GROUP[]`                                                                                                                                                         | -                         | Группы вариантов                                  |
| [`size?`](#размер)                                                      | `'xs'`, `'s'`, `'m'`, `'l'`                                                                                                                                       | `'m'`                     | Размер компонента                                 |
| [`form?`](#форма)                                                       | `default`, `brick`, `round`, `clearRound`, `roundClear`, `clearDefault`, `defaultClear`, `defaultBrick`, `brickDefault`, `brickClear`, `clearBrick`, `clearClear` | `default`                 | Форма компонента                                  |
| [`view?`](#стиль)                                                       | `default`, `clear`                                                                                                                                                | `default`                 | Стиль компонента                                  |
| [`status?`](#статус-поля)                                               | `'', 'alert', 'success', 'warning'`                                                                                                                               | `' '`                     | Статус поля                                       |
| [`disabled?`](#неактивный-выпадающий-список)                            | `boolean`                                                                                                                                                         | -                         | Делает компонент неактивным                       |
| [`isLoading?`](#индикация-загрузки)                                     | `boolean`                                                                                                                                                         | -                         | Отображает индикатор загрузки                     |
| [`virtualScroll?`](#виртуализация-списка)                               | `boolean`                                                                                                                                                         | -                         | Включает виртуализацию скролла                    |
| [`renderItem?`](#кастомный-элемент-списка)                              | `SelectPropRenderItem<ITEM>`                                                                                                                                      | рендер по умолчанию       | Рендер-функция для элемента списка                |
| [`renderValue?`](#кастомное-выбранное-значение)                         | `SelectPropRenderValue<ITEM>`                                                                                                                                     | рендер по умолчанию       | Рендер-функция для выбранного значения            |
| [`getItemLabel?`](#кастомные-типы-данных)                               | `SelectPropGetItemLabel<ITEM>`                                                                                                                                    | `(item) => item.label`    | Функция для получения названия элемента           |
| [`getItemKey?`](#кастомные-типы-данных)                                 | `SelectPropGetItemKey<ITEM>`                                                                                                                                      | `(item) => item.id`       | Функция для получения ключа элемента              |
| [`getItemGroupKey?`](#кастомные-типы-данных)                            | `SelectPropGetItemGroupKey<ITEM>`                                                                                                                                 | `(item) => item.groupId`  | Функция для получения ключа группы                |
| [`getItemDisabled?`](#кастомные-типы-данных)                            | `SelectPropGetItemDisabled<ITEM>`                                                                                                                                 | `(item) => item.disabled` | Функция для определения неактивного элемента      |
| [`getGroupLabel?`](#кастомные-типы-данных)                              | `SelectPropGetGroupLabel<GROUP>`                                                                                                                                  | `(group) => group.label`  | Функция для получения названия группы             |
| [`getGroupKey?`](#кастомные-типы-данных)                                | `SelectPropGetGroupKey<GROUP>`                                                                                                                                    | `(group) => group.id`     | Функция для получения ключа группы                |
| [`dropdownOpen?`](#управление-состоянием-выпадающего-списка)            | `boolean`                                                                                                                                                         | -                         | Состояние выпадающего списка (открыт/закрыт)      |
| [`onDropdownOpen?`](#управление-состоянием-выпадающего-списка)          | `(open: boolean) => void`                                                                                                                                         | -                         | Обработчик открытия/закрытия списка               |
| [`ignoreOutsideClicksRefs?`](#управление-состоянием-выпадающего-списка) | `ReadonlyArray<React.RefObject<HTMLElement>>`                                                                                                                     | -                         | Ссылки на элементы, клики по которым игнорируются |
| [`onScrollToBottom?`](#виртуализация-списка)                            | `(length: number) => void`                                                                                                                                        | -                         | Обработчик прокрутки до конца списка              |
| [`required?`](#поведение)                                               | `boolean`                                                                                                                                                         | -                         | Обязательность заполнения                         |
| `labelForEmptyItems?`                                                   | `string`                                                                                                                                                          | `Список пуст`             | Текст, если список пуст                           |
| `onBlur?`                                                               | `(event?: React.FocusEvent<HTMLInputElement>) => void`                                                                                                            | -                         | Обработчик события `blur`                         |
| `onFocus?`                                                              | `(event?: React.FocusEvent<HTMLInputElement>) => void`                                                                                                            | -                         | Обработчик события `focus`                        |
| `ariaLabel?`                                                            | `string`                                                                                                                                                          | -                         | `aria` атрибут для поля ввода                     |
| `name?`                                                                 | `string`                                                                                                                                                          | -                         | Имя поля ввода                                    |
| `className`                                                             | `string`                                                                                                                                                          | -                         | Дополнительный CSS-класс                          |
| `dropdownClassName`                                                     | `string`                                                                                                                                                          | -                         | Дополнительный CSS-класс для выпадающего блока    |
| `ref`                                                                   | `React.Ref<HTMLDivElement>`                                                                                                                                       | -                         | Ссылка на корневой DOM-элемент компонента         |

## Содержимое

### Варианты

Свойство `items` определяет массив элементов выпадающего списка. По умолчанию тип `items` соответствует интерфейсу `SelectItemDefault`:

- `label` — название элемента (пишите с заглавной буквы, без точки в конце).
- `id` — уникальный ключ элемента.
- `groupId` — идентификатор группы, если элементы разбиты на группы.
- `disabled` — флаг неактивного элемента.

<MdxTabs>

<SelectExampleItems />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return <Select items={items} value={value} onChange={setValue} />;
```

</MdxTabs>

### Выбранное значение

Свойство `value` задаёт выбранное значение из массива `items`. Для обработки выбора используется `onChange`, который возвращает выбранный элемент и событие. Выбор срабатывает по клику или нажатию `Enter`.

<MdxTabs>

<SelectExampleItems />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return <Select items={items} value={value} onChange={setValue} />;
```

</MdxTabs>

### Лейбл и подпись

Свойство `label` добавляет лейбл, `labelPosition` задаёт его положение (`'top'` или `'left'`), `caption` — подпись под компонентом, `labelIcon` — иконка для лейбла.

<MdxTabs>

<SelectExampleLabel />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return (
  <Select
    label="Значение"
    caption={value?.label}
    placeholder="Выберите значение"
    items={items}
    value={value}
    onChange={setValue}
  />
);
```

</MdxTabs>

<MdxTabs>

<SelectExampleLabelIcon />

```tsx
<Select label="Значение" labelIcon={IconQuestion} />
```

</MdxTabs>

### Подсказка

Свойство `placeholder` задаёт текст, отображаемый, когда ничего не выбрано. Рекомендуется использовать понятный текст, например, **"Выберите"**.

<MdxTabs>

<SelectExamplePlaceholder />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Выберите значение"
    items={items}
    value={value}
    onChange={setValue}
  />
);
```

</MdxTabs>

### Неактивный элемент

Чтобы сделать элемент списка неактивным, добавьте `disabled: true` в объект элемента в массиве `items`.

<MdxTabs>

<SelectExampleDisabledItem />

```tsx
const items = [
  { label: 'Первый', id: 1, disabled: true },
  { label: 'Второй', id: 2, disabled: false },
  { label: 'Третий', id: 3, disabled: true },
];

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Выберите значение"
    items={items}
    value={value}
    onChange={setValue}
  />
);
```

</MdxTabs>

### Группы вариантов

Для группировки элементов используйте свойство `groups`, задающее массив групп. Тип группы по умолчанию — `SelectGroupDefault` с полями `label` и `id`. Элементы связываются с группами через `groupId`.

<MdxTabs>

<SelectExampleGroups />

```tsx
const groups = [
  { id: 1, label: 'Первая группа' },
  { id: 2, label: 'Вторая группа' },
  { id: 3, label: 'Третья группа' },
];

const items = [
  { label: 'Первый', id: 1, groupId: 1 },
  { label: 'Второй', id: 2, groupId: 2 },
  { label: 'Третий', id: 3, groupId: 1 },
  { label: 'Четвертый', id: 4, groupId: 3 },
  { label: 'Пятый', id: 5, groupId: 2 },
];

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Выберите значение"
    items={items}
    value={value}
    onChange={setValue}
    groups={groups}
  />
);
```

</MdxTabs>

## Внешний вид

### Размер

Свойство `size` задаёт размер компонента. Варианты: `'xs'`, `'s'`, `'m'`, `'l'`. По умолчанию — `'m'`. Компонент занимает всю ширину родительского контейнера.

<MdxTabs>

<SelectExampleSize />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Размер xs"
    size="xs"
    items={items}
    value={value}
    onChange={setValue}
  />
);
```

</MdxTabs>

### Форма

Свойство `form` определяет форму компонента. Варианты: `default`, `brick`, `round`, `clearRound`, `roundClear`, `clearDefault`, `defaultClear`, `defaultBrick`, `brickDefault`, `brickClear`, `clearBrick`, `clearClear`. По умолчанию — `default`.

<MdxTabs>

<SelectExampleForm />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return (
  <>
    <Select
      placeholder="Форма round"
      form="round"
      items={items}
      value={value}
      onChange={setValue}
    />
    <Select
      placeholder="Форма brickDefault"
      form="brickDefault"
      items={items}
      value={value}
      onChange={setValue}
    />
  </>
);
```

</MdxTabs>

### Стиль

Свойство `view` задаёт стиль компонента. Варианты: `default`, `clear`. По умолчанию — `default`. Стиль `clear` используется для кастомных полей или обёртывания в контейнер.

<MdxInformer status="normal">

Для создания hover-эффекта при `view="clear"` управляйте стилем контейнера, в котором находится **Select**.

</MdxInformer>

<MdxTabs>

<SelectExampleView />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Выберите значение"
    view="clear"
    items={items}
    value={value}
    onChange={setValue}
  />
);
```

</MdxTabs>

### Статус поля

Свойство `status` выделяет поле визуально. Варианты: `''`, `'alert'`, `'success'`, `'warning'`. По умолчанию — `''` (обычное поле).

<MdxTabs>

<SelectExampleStatus />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return (
  <>
    <Select
      placeholder="default"
      caption="default"
      size="s"
      items={items}
      value={value}
      onChange={setValue}
    />
    <Select
      placeholder="alert"
      caption="alert"
      status="alert"
      size="s"
      items={items}
      value={value}
      onChange={setValue}
    />
    <Select
      placeholder="warning"
      caption="warning"
      status="warning"
      size="s"
      items={items}
      value={value}
      onChange={setValue}
    />
    <Select
      placeholder="success"
      caption="success"
      size="s"
      status="success"
      items={items}
      value={value}
      onChange={setValue}
    />
  </>
);
```

</MdxTabs>

## Поведение

### Неактивный выпадающий список

Свойство `disabled` делает весь компонент неактивным.

<MdxTabs>

<SelectExampleDisabled />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Выберите значение"
    items={items}
    value={value}
    onChange={setValue}
    disabled
  />
);
```

</MdxTabs>

### Управление состоянием выпадающего списка

Для управления состоянием списка используйте:

- `dropdownOpen` — флаг открытия/закрытия.
- `onDropdownOpen` — обработчик открытия/закрытия.
- `ignoreOutsideClicksRefs` — список элементов, клики по которым игнорируются.

<MdxTabs>

<SelectExampleDropdownOpen />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const Icon = withAnimateSwitcherHOC({
  startIcon: IconArrowDown,
  startDirection: 0,
  endDirection: 180,
});

const [value, setValue] = useState(null);
const [open, setOpen] = useFlag();
const buttonRef = useRef(null);
const inputRef = useRef(null);
const onDropdownOpen = (open) => {
  setOpen.set(open);
  if (open) {
    inputRef.current?.focus();
  }
};

return (
  <AnimateIconSwitcherProvider active={open}>
    <FieldGroup>
      <Button
        ref={buttonRef}
        label={open ? 'Закрыть' : 'Открыть'}
        onClick={setOpen.toggle}
        iconLeft={Icon}
        onlyIcon
      />
      <Select
        inputRef={inputRef}
        placeholder="Выберите вариант"
        items={items}
        value={value}
        onChange={setValue}
        dropdownOpen={open}
        onDropdownOpen={onDropdownOpen}
        ignoreOutsideClicksRefs={[buttonRef]}
      />
    </FieldGroup>
  </AnimateIconSwitcherProvider>
);
```

</MdxTabs>

### Индикация загрузки

Свойство `isLoading` включает индикатор загрузки. Используется для асинхронной загрузки данных.

<MdxTabs>

<SelectExampleIsLoading />

```tsx
const useMockLoadData = (searchValue = '', timeout = 2000) => {
  const [isLoading, setIsLoading] = useFlag();
  const [isStart, setIsStart] = useFlag();
  const [length, setLength] = useState(0);
  const isLoadingOffDebounce = useDebounce(setIsLoading.off, 2000);

  const items = useMemo(() => {
    if (searchValue) {
      return new Array(10000)
        .fill(null)
        .map((_, i) => ({
          label: `Опция ${i + 1}`,
          id: i,
        }))
        .filter(
          (item) =>
            item.label
              .toLocaleLowerCase()
              .indexOf(searchValue.toLocaleLowerCase()) !== -1,
        );
    }
    return new Array(length).fill(null).map((_, i) => ({
      label: `Опция ${i + 1}`,
      id: i,
    }));
  }, [length, searchValue]);

  useEffect(() => {
    setIsLoading.on();
    isLoadingOffDebounce();
  }, [searchValue]);

  useEffect(() => {
    if (isStart) {
      setIsLoading.on();
    }
    const timeoutNumber = setTimeout(() => {
      if (isStart) {
        setLength((state) => state + 100);
        setIsLoading.off();
        setIsStart.off();
      }
    }, timeout);
    return () => clearTimeout(timeoutNumber);
  }, [isStart]);

  useEffect(() => {
    return () => {
      setIsLoading.off();
      setIsStart.off();
    };
  }, []);

  return [searchValue && isLoading ? [] : items, isLoading, setIsStart.on];
};

const [value, setValue] = useState(null);
const [data, isLoading, load] = useMockLoadData();
const onDropdownOpen = (open) => open && load();

return (
  <Select
    placeholder="Кликните по полю ввода, чтобы данные загрузились"
    items={data}
    value={value}
    onChange={setValue}
    onDropdownOpen={onDropdownOpen}
    isLoading={isLoading}
    virtualScroll
    onScrollToBottom={load}
  />
);
```

</MdxTabs>

### Виртуализация списка

Свойство `virtualScroll` включает виртуализацию для оптимизации отображения больших списков.

<MdxTabs>

<SelectExampleVirtualScroll />

```tsx
const items = new Array(100000).fill(null).map((_, i) => ({
  label: `Опция ${i + 1}`,
  groupId: Math.floor(i / 100) + 1,
  id: i,
}));

const groups = new Array(items.length / 100).fill(null).map((_, i) => ({
  id: i + 1,
  label: `Группа ${i + 1}`,
}));

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Чтобы посмотреть весь список, прокрутите вниз"
    items={items}
    value={value}
    onChange={setValue}
    groups={groups}
    virtualScroll
  />
);
```

</MdxTabs>

## Кастомизация

### Кастомный элемент списка

Свойство `renderItem` позволяет настроить отображение элементов списка через функцию рендера.

<MdxInformer status="warning">

У корневого DOM-элемента в `renderItem` должно быть `role="option"`.

</MdxInformer>

<MdxTabs>

<SelectExampleRenderItem />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Выберите значение"
    items={items}
    value={value}
    onChange={setValue}
    renderItem={({ item, active, hovered, onClick, onMouseEnter, ref }) => (
      <div
        className={cnSelectExampleRenderItem('Item', { active, hovered })}
        aria-selected={active}
        onMouseEnter={onMouseEnter}
        onClick={onClick}
        ref={ref}
      >
        {item.label}
      </div>
    )}
  />
);
```

</MdxTabs>

### Кастомное выбранное значение

Свойство `renderValue` позволяет настроить отображение выбранного значения через функцию рендера.

<MdxTabs>

<SelectExampleRenderValue />

```tsx
const items = [
  { label: 'Первый', id: 1 },
  { label: 'Второй', id: 2 },
  { label: 'Третий', id: 3 },
];

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Выберите значение"
    items={items}
    value={value}
    onChange={setValue}
    renderValue={({ item }) => (
      <div>
        <span role="img" aria-label="Panda">
          🐼
        </span>{' '}
        — {item.name}
      </div>
    )}
  />
);
```

</MdxTabs>

### Кастомные типы данных

Для кастомных типов данных в `items` и `groups` используйте геттеры: `getItemLabel`, `getItemKey`, `getItemGroupKey`, `getItemDisabled`, `getGroupLabel`, `getGroupKey`.

<MdxInformer status="warning">

Без геттеров используются значения по умолчанию, которые могут не соответствовать вашим типам данных.

</MdxInformer>

<MdxTabs>

<SelectExampleCustomTypes />

```tsx
const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Выберите значение"
    items={['Первый', 'Второй', 'Третий']}
    value={value}
    onChange={setValue}
    getItemKey={(item) => item}
    getItemLabel={(item) => item}
  />
);
```

</MdxTabs>

<MdxTabs>

<SelectExampleCustomTypesWithGroups />

```tsx
const items = [
  { name: 'Первый', group: 'Первая группа' },
  { name: 'Второй', group: 'Третья группа' },
  { name: 'Третий', group: 'Вторая группа' },
  { name: 'Четвертый', group: 'Первая группа' },
  { name: 'Пятый', group: 'Первая группа' },
  { name: 'Шестой', group: 'Третья группа' },
  { name: 'Седьмой', group: 'Первая группа' },
  { name: 'Восьмой', group: 'Вторая группа' },
  { name: 'Девятый', group: 'Третья группа' },
  { name: 'Десятый', group: 'Вторая группа' },
];

const groups = ['Первая группа', 'Вторая группа', 'Третья группа'];

const [value, setValue] = useState(null);
return (
  <Select
    placeholder="Выберите значение"
    items={items}
    groups={groups}
    value={value}
    onChange={setValue}
    getItemKey={(item) => item.name}
    getItemLabel={(item) => item.name}
    getItemGroupKey={(item) => item.group}
    getGroupKey={(group) => group}
    getGroupLabel={(group) => group}
  />
);
```

</MdxTabs>
