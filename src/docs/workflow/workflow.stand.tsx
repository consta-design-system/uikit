import { createStand } from '##/stand/standConfig';

export default createStand({
  title: 'Порядок работы с Consta',
  id: 'workflow',
  group: 'workflow',
  status: 'stable',
  version: '4.0.0',
  order: 10,
});
