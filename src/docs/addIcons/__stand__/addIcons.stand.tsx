import { createStand } from '##/stand/standConfig';

export default createStand({
  title: 'Создание иконок',
  id: 'addIcons',
  group: 'custom',
  status: 'stable',
  version: '4.0.0',
  order: 30,
});
