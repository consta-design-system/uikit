```tsx
import { useComponentSize } from '@consta/uikit/useComponentSize';
```

Хук `useComponentSize` подписывается на изменение размеров передаваемого элемента и возвращает его ширину и высоту.

Данный хук нужен для отслеживания размеров конкретного элемента DOM-дерева. Если задача состоит в адаптации контента, лучше используйте хук [useComponentBreakpoints](##LIBS.LIB.STAND/lib:uikit/stand:hooks-useсomponentbreakpoints-stable), так как с ним процесс ререндера происходит только при пересечении контрольной точки.

```tsx
import { useComponentSize } from '@consta/uikit/useComponentSize';

...

const containerRef = useRef<HTMLDivElement>(null);
const { width, height } = useComponentSize(containerRef);

return <div ref={containerRef}>...</div>;
```

## Свойства

| Аргумент | Тип и значения                                                        | По умолчанию | Описание          |
| -------- | --------------------------------------------------------------------- | ------------ | ----------------- |
| `ref`    | `React.RefObject<HTMLElement`<code>&#124;</code>`SVGGraphicsElement>` | -            | Ссылка на элемент |
