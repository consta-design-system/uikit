import {
  MixSpaceExampleButtonWithSpaces,
  MixSpaceExampleButtonWithoutSpaces,
} from './examples/MixSpaceExample/MixSpaceExample';

```tsx
import { cnMixSpace } from '@consta/uikit/MixSpace';
```

С помощью миксина `MixSpace` можно добавить отступы внутри
любого элемента и вокруг него.

##### Пример кнопки без отступов

```tsx
export const MixSpaceExampleButtonWithoutSpaces = () => {
  return (
    <>
      <Text>Это текст, он нужен, чтобы показать отступы от кнопки сверху</Text>
      <Button label="Это кнопка без отступов" />
      <Text>Это текст, он нужен, чтобы показать отступы от кнопки снизу</Text>
    </>
  );
};
```

<MixSpaceExampleButtonWithoutSpaces />

##### Пример кнопки с отступами снаружи

```tsx
export const MixSpaceExampleButtonWithSpaces = () => {
  return (
    <>
      <Text>Это текст, он нужен, чтобы показать отступы от кнопки сверху</Text>
      <Button
        label="Это кнопка с отступами снаружи"
        className={cnMixSpace({
          m: 'm',
          mT: '3xs',
        })}
      />
      <Text>Это текст, он нужен, чтобы показать отступы от кнопки снизу</Text>
    </>
  );
};
```

<MixSpaceExampleButtonWithSpaces />

## Импорт

```tsx
import { cnMixSpace } from '@consta/uikit/MixSpace';
```

## Список свойств

```tsx
type Space =
  | 0
  | 'auto'
  | '3xs'
  | '2xs'
  | 'xs'
  | 's'
  | 'm'
  | 'l'
  | 'xl'
  | '2xl'
  | '3xl'
  | '4xl'
  | '5xl'
  | '6xl';
```

| Свойство | Тип или варианты значения | По умолчанию | Описание                               |
| -------- | ------------------------- | ------------ | -------------------------------------- |
| `p?`     | `Space`                   | -            | Отступы внутри элемента                |
| `m?`     | `Space`                   | -            | Отступы вокруг элемента                |
| `pV?`    | `Space`                   | -            | Вертикальные отступы внутри элемента   |
| `pH?`    | `Space`                   | -            | Горизонтальные отступы внутри элемента |
| `mV?`    | `Space`                   | -            | Вертикальные отступы от элемента       |
| `mH?`    | `Space`                   | -            | Горизонтальные отступы от элемента     |
| `mT?`    | `Space`                   | -            | Отступ сверху от элемента              |
| `mL?`    | `Space`                   | -            | Отступ слева от элемента               |
| `mR?`    | `Space`                   | -            | Отступ справа от элемента              |
| `mB?`    | `Space`                   | -            | Отступ снизу от элемента               |
| `pT?`    | `Space`                   | -            | Отступ сверху внутри элемента          |
| `pL?`    | `Space`                   | -            | Отступ слева внутри элемента           |
| `pR?`    | `Space`                   | -            | Отступ справа внутри элемента          |
| `pB?`    | `Space`                   | -            | Отступ снизу внутри элемента           |

## Пример использования

```tsx
import React from 'react';

import { Text } from '@consta/uikit/Text';
import { cnMixSpace } from '@consta/uikit/MixSpace';

export const MixSpaceExample = () => {
  return (
    <div
      className={cnMixSpace({
        p: 'xl',
        pH: '3xl',
        m: 'm',
        mT: '3xs',
      })}
    >
      <Text>Здесь может быть ваш текст</Text>
    </div>
  );
};
```
